"""Deep systems architectural walkthroughs and mechanics for all 29 Kubelings reference guides.

Explains gRPC handoffs, Protobuf/JSON serialization, Linux kernel datapath,
cgroups v2, network routing, admission webhooks, and cloud control plane interactions.
"""

CHAPTER_EXPLANATIONS: dict[int, dict[str, str]] = {
    1: {
        "flow": '1. **Manifest Ingestion & OpenAPI Deserialization**: The user or CI client submits a YAML Pod manifest via `kubectl apply`. The `kube-apiserver` deserializes the YAML into native Go structs (`v1.Pod`), validates the fields against OpenAPI v3 schemas, and applies admission checks.\n2. **Persistence in etcd**: Upon passing validation, `kube-apiserver` encodes the Go struct into a compact **Protobuf binary payload** and executes an atomic MVCC transaction to persist the record at key `/registry/pods/{namespace}/{name}` in `etcd3`.\n3. **Scheduling Watch & Node Binding**: `kube-scheduler` maintains an HTTP/2 streaming watch on unassigned Pods (`spec.nodeName == ""`). It filters and scores candidate nodes, selecting the optimal worker node, and writes an asynchronous `Binding` subresource back to the API server.\n4. **Kubelet Sync Loop & PLEG**: The target worker node\'s `kubelet` detects the scheduled Pod via its `PodConfig` channel. The Pod Lifecycle Event Generator (PLEG) detects state divergence and initiates container reconciliation.\n5. **CRI gRPC Sandbox Creation**: `kubelet` connects to the Container Runtime Interface (CRI) engine (`containerd` or `CRI-O`) via a local Unix Domain Socket (`/run/containerd/containerd.sock`) over **gRPC**. It issues a `RunPodSandbox` gRPC request, prompting `containerd` to spawn the Linux **pause container** (`CLONE_NEWNET`, `CLONE_NEWUTS`, `CLONE_NEWIPC`).\n6. **Sequential Init Container Execution**: `kubelet` invokes `CreateContainer` and `StartContainer` over CRI gRPC for each init container in strict sequential order. Each init container must terminate with exit code 0 before subsequent containers are scheduled.\n7. **Main & Sidecar Container Startup**: `kubelet` initiates the application and sidecar containers concurrently within the shared network and IPC namespaces, attaching mounted storage volumes (`emptyDir`, `ConfigMap`, or persistent block mounts).',
        "serialization_protocols": "- **gRPC over Unix Domain Sockets (UDS)**: `kubelet` communicates with `containerd` via `/run/containerd/containerd.sock` using the `runtime.v1.RuntimeService` gRPC interface.\n- **Protobuf Serialization**: Data exchanged over CRI gRPC and stored within `etcd3` is serialized using Protocol Buffers (proto3) to eliminate JSON parsing overhead and minimize CPU/memory serialization latency.\n- **JSON/YAML Deserialization & Scheme Conversion**: `kubectl` transmits JSON payloads over HTTP/2 with TLS 1.3 to `kube-apiserver`. The API server uses `k8s.io/apimachinery` codec schemes to convert external versions (`v1`) into internal unstructured objects before validation.",
        "components": "- **kube-apiserver**: Stateless HTTP REST gateway providing OpenAPI v3 schema validation, authentication (AuthN), authorization (AuthZ), admission mutation/validation, and `etcd` persistence.\n- **kubelet & PLEG**: Node management agent running as a systemd service. PLEG (Pod Lifecycle Event Generator) polls runtime relists and container events to maintain internal active pod caches.\n- **Pause Container**: A minimal C program (`pause.c`) that sleeps indefinitely (`pause()`), holding the Linux network (`CLONE_NEWNET`), IPC (`CLONE_NEWIPC`), and UTS namespaces open for all co-located containers in the Pod.\n- **Shared Volume Subsystem**: Linux VFS bind-mounts configured by kubelet's Volume Manager, enabling atomic inter-container data exchange via memory-backed `tmpfs` or host filesystem mounts.",
        "mechanics_failures": "- **cgroups v2 Enforcement**: Resource limits translate directly to Linux control group v2 files: `spec.containers[*].resources.limits.cpu` sets `cpu.max` (quota and period in microseconds), while `limits.memory` sets `memory.max` (hard memory ceiling).\n- **OOM Killer & `oom_score_adj`**: The Linux kernel Out-Of-Memory (OOM) killer uses `/proc/[pid]/oom_score_adj`. Kubelet assigns `-997` to Guaranteed QoS Pods, while BestEffort Pods receive `1000`, making BestEffort workloads the first candidates for termination under memory exhaustion.\n- **Init Container Failure Modes**: If any init container exits with non-zero status and `restartPolicy: Always`, kubelet restarts the failed init container using exponential backoff (10s, 20s, up to 5 minutes), blocking application startup indefinitely.",
    },
    2: {
        "flow": "1. **Declarative State Declaration**: A platform operator submits a `Deployment` manifest declaring `spec.replicas: 3` and `spec.strategy.type: RollingUpdate`.\n2. **Informer Reflector List-Watch**: The `DeploymentController` inside `kube-controller-manager` runs a `SharedIndexInformer`. The informer's `Reflector` establishes an HTTP/2 chunked List-Watch connection to `kube-apiserver`, streaming delta changes into a thread-safe local `Indexer` FIFO queue.\n3. **Reconciliation Loop & WorkQueue**: The controller pops the Deployment key from the `RateLimitingWorkQueue`. It queries the local cache for matching ReplicaSets via label selectors (`matchLabels`).\n4. **RollingUpdate Rollout Math**: When `spec.template.spec` changes, the controller creates a new Revision ReplicaSet (`v2`). It calculates `maxSurge` (temporary extra pods allowed above desired count) and `maxUnavailable` (maximum pods allowed in non-ready state), progressively incrementing `spec.replicas` on `v2` while decrementing `v1`.\n5. **ReplicaSet Controller Action**: The `ReplicaSetController` observes the updated replica counts, compares desired vs actual live Pod counts, and executes batch HTTP POST requests to `kube-apiserver` to spawn or terminate individual `v1.Pod` objects.\n6. **Self-Healing & Drift Convergence**: If a node crashes or a worker process is terminated, the informer receives a `Delete` event, immediately re-enqueuing the Deployment key to recreate replacement Pods until the observed state equals the desired state.",
        "serialization_protocols": "- **HTTP/2 Chunked Streaming (Watch API)**: The API server streams resource change events (`ADDED`, `MODIFIED`, `DELETED`) encoded as JSON or binary Protobuf over long-lived HTTP/2 streams.\n- **Optimistic Concurrency Control (OCC)**: State updates use HTTP `PUT` with `metadata.resourceVersion`. If another controller modified the object in `etcd` concurrently, the API server rejects the write with `409 Conflict`, prompting the controller to re-fetch the latest state and retry.\n- **Protobuf Internal Wire Protocol**: Inter-component communication between `kube-controller-manager` and `kube-apiserver` utilizes Protobuf encoding to support high-throughput event processing across large clusters.",
        "components": "- **kube-controller-manager**: Monolithic Go binary running a collection of distinct control loops (Deployment, ReplicaSet, StatefulSet, Job, Node) managed by a leader-election lock in `coordination.k8s.io/leases`.\n- **SharedIndexInformer & DeltaFIFO**: Caching and event distribution layer that reduces `kube-apiserver` query loads by maintaining an in-memory thread-safe copy of cluster resources indexed by namespace and labels.\n- **RateLimitingWorkQueue**: Go workqueue implementation providing exponential backoff, token-bucket rate limiting, and deduplication of concurrent reconciliation keys.\n- **ReplicaSet Reconciler**: Dedicated controller responsible exclusively for maintaining the exact number of active, ready Pods matching a given Pod template and selector.",
        "mechanics_failures": '- **Cascading Rollout Stall (`ProgressDeadlineSeconds`)**: If new revision Pods fail startup or readiness checks, the rollout stalls. If the condition persists beyond `progressDeadlineSeconds` (default: 600s), the Deployment controller transitions `.status.conditions[Type=Progressing].status = "False"` with reason `ProgressDeadlineExceeded`.\n- **Label Selector Mutability Invariants**: `spec.selector` is immutable after creation. Attempting to modify `matchLabels` requires recreating the Deployment, as selector collisions can lead to orphaned ReplicaSets or split-brain ownership loops.\n- **Split-Brain Prevention via Leases**: Only the active leader instance of `kube-controller-manager` executes reconciliation routines. Non-leader instances maintain active informer caches and continuously renew heartbeat leases to take over within 15 seconds upon leader termination.',
    },
    3: {
        "flow": "1. **Secret & Config Creation**: Operators submit `v1/ConfigMap` and `v1/Secret` manifests containing key-value configurations and base64-encoded credentials.\n2. **KMS Envelope Encryption at Rest**: `kube-apiserver` receives the Secret payload. If configured with a Key Management Service (KMS v2 provider), it generates a local Data Encryption Key (DEK), encrypts the secret data using AES-GCM-256, encrypts the DEK with the remote KMS Key Encryption Key (KEK) via gRPC, and stores the envelope ciphertext in `etcd`.\n3. **Kubelet Volume Manager Mounting**: When a Pod referencing the ConfigMap/Secret is scheduled to a node, the local `kubelet` Volume Manager detects the dependency and fetches the plain object via API watch.\n4. **Atomic Symlink Projection**: `kubelet` writes the keys as individual files inside a timestamped directory on the host (e.g., `/var/lib/kubelet/pods/<UID>/volumes/kubernetes.io~secret/my-secret/..2026_09_02_12_00_00.123456789`). It atomically swaps a symlink named `..data` to point to the new directory.\n5. **Dynamic Updates vs Static Env Injection**:\n   - **Mounted Files**: Application containers reading from the mount path automatically see updated file contents whenever the `..data` symlink is swapped, without container restarts.\n   - **Environment Variables (`envFrom`)**: Injected into Linux process memory table (`/proc/1/environ`) during `clone()` at container boot; values remain static until the container process is restarted.",
        "serialization_protocols": "- **KMS v2 gRPC Protocol**: `kube-apiserver` communicates with KMS plugins over local Unix domain sockets using `k8s.io/kms/apis/v2` gRPC definitions for `Encrypt` and `Decrypt` remote procedure calls.\n- **Base64 RFC 4648 Encoding**: Secrets in YAML/JSON are Base64 encoded for binary data representation across text transports, but this provides zero cryptographic confidentiality without TLS and encryption-at-rest.\n- **Protobuf Storage Compression**: `etcd` stores ConfigMaps and Secrets as serialized Protocol Buffer messages under the `/registry/configmaps` and `/registry/secrets` keyspace.",
        "components": "- **kubelet VolumeManager**: Internal loop within kubelet responsible for orchestrating `MountVolume`, `UnmountVolume`, and syncing projected volume payloads against API server state.\n- **KMS Plugin Daemon**: Out-of-tree gRPC daemon (supporting AWS KMS, HashiCorp Vault, GCP Cloud KMS, or Azure Key Vault) that handles hardware security module (HSM) cryptographic operations.\n- **Atomic Symlink Tree**: File system layout utilizing Linux `symlink()` and `rename()` system calls to prevent race conditions during multi-file configuration updates.\n- **Linux `/proc/[pid]/environ`**: Kernel table storing initial process environment variables created during `execve`, isolated per process namespace.",
        "mechanics_failures": "- **Stale Memory Footprints with SubPath Mounts**: Using `volumeMounts.subPath` bypasses the atomic `..data` symlink tree by bind-mounting a single file directly. Consequently, subPath mounted files will **never** receive automatic live updates when ConfigMaps change.\n- **KMS Plugin Unavailability**: If the KMS gRPC daemon crashes or network connectivity to the cloud KMS endpoint drops, `kube-apiserver` cannot decrypt Secrets, causing Pod scheduling on new nodes to fail with `CreateContainerConfigError`.\n- **Secret Size Limits**: Individual `etcd` key-value pairs are hard-capped at 1.5MB by default, restricting individual ConfigMaps and Secrets to a maximum aggregate payload size of 1MB to prevent etcd transaction log saturation.",
    },
    4: {
        "flow": "1. **PVC Creation & StorageClass Matching**: A developer declares a `PersistentVolumeClaim` (PVC) specifying storage requests (e.g., `100Gi`) and `storageClassName: ebs-gp3-sc`.\n2. **External Provisioner Detection**: The `csi-provisioner` sidecar watching PVCs detects an unbound claim. If `volumeBindingMode: WaitForFirstConsumer` is set, dynamic provisioning waits until `kube-scheduler` selects an eligible node to ensure volume placement matches compute topology.\n3. **Dynamic Volume Provisioning via CSI gRPC**: `csi-provisioner` issues a `CreateVolume` gRPC call to the cloud storage provider plugin (e.g., AWS EBS CSI Controller). The plugin communicates with cloud APIs to allocate the underlying physical EBS volume or SAN LUN.\n4. **PV Creation & Two-Way Binding**: `csi-provisioner` writes a corresponding `PersistentVolume` (PV) object to `kube-apiserver`. The `PersistentVolumeController` binds the PVC and PV bidirectionally (`spec.volumeName` and `spec.claimRef`).\n5. **Node Attachment (`AttachVolume`)**: The `csi-attacher` controller calls `ControllerPublishVolume` over gRPC, prompting the cloud API to attach the EBS volume block device to the target EC2 worker instance.\n6. **Node Formatting & Mount (`NodePublishVolume`)**: The worker node's `kubelet` detects the attached block device (e.g., `/dev/nvme1n1`). It calls the local `csi-node` daemon via gRPC `NodeStageVolume` (formats with `ext4`/`xfs` if unformatted) and `NodePublishVolume` (bind-mounts the filesystem to `/var/lib/kubelet/pods/<UID>/volumes/kubernetes.io~csi/<vol-name>/mount`).\n7. **Container Bind-Mount**: The container runtime launches the application container with an explicit Linux bind-mount mapping the host target path to the container's designated mount path (e.g., `/var/lib/data`).",
        "serialization_protocols": "- **Container Storage Interface (CSI v1.x) gRPC**: All storage controller and node communications execute over standard gRPC interfaces defined by `container-storage-interface/spec`.\n- **Unix Domain Socket RPC**: Node-level CSI plugins listen on host sockets (such as `/var/lib/kubelet/plugins/ebs.csi.aws.com/csi.sock`) registered with kubelet via the CSI Plugin Registration mechanism.\n- **Protobuf Wire Payloads**: Volume capabilities, mount flags, access modes (`ReadWriteOnce`, `ReadWriteMany`), and cloud volume IDs are passed as structured protobuf messages across controller boundaries.",
        "components": "- **csi-provisioner**: Kubernetes SIG sidecar container that watches PVCs and calls CSI `CreateVolume` and `DeleteVolume`.\n- **csi-attacher**: Sidecar container that watches `VolumeAttachment` API objects and calls CSI `ControllerPublishVolume` and `ControllerUnpublishVolume`.\n- **csi-node Plugin**: DaemonSet running on every node that executes privileged OS operations (formatting filesystems via `mkfs`, creating Linux mount namespaces).\n- **kubelet Volume Manager**: Reconciler inside kubelet that verifies volume attachments, invokes local CSI node RPCs, and maintains desired-state mount maps.",
        "mechanics_failures": "- **Multi-Attach Error (`VolumeAttachmentConflict`)**: Cloud block volumes (like AWS EBS or GCP Persistent Disk) with `ReadWriteOnce` (RWO) cannot be attached to multiple physical nodes simultaneously. If a Pod is rescheduled while the old node has not detached the volume, the Pod hangs in `ContainerCreating` with `Multi-Attach error for volume`.\n- **Filesystem Corruption on Unclean Unmount**: Sudden node termination without graceful CSI `NodeUnpublishVolume` execution can leave orphan block attachments and stale lock files, requiring manual cloud console detaches or node reboots.\n- **Capacity Exhaustion & Inode Depletion**: If an ext4/xfs volume fills to 100% capacity or exhausts available inodes, the application container encounters `No space left on device` (ENOSPC), causing database write stalls and container crashes.",
    },
    5: {
        "flow": "1. **Service Registration & VIP Allocation**: An operator creates a `v1/Service` with `spec.type: ClusterIP` and selector `app: payment-api`. `kube-apiserver` assigns an immutable Virtual IP (VIP) from the preconfigured CIDR block (e.g., `10.96.0.0/12`).\n2. **EndpointSlice Controller Reconciliation**: The `EndpointSlice` controller inside `kube-controller-manager` queries the informer cache for all running Pods whose labels match `app: payment-api` and whose `status.conditions[Ready]` is `True`. It populates `discovery.k8s.io/v1 EndpointSlice` objects containing lists of direct Pod CNI IP addresses and ports.\n3. **CoreDNS A-Record Resolution**: When a client Pod executes an HTTP request to `http://payment-api.default.svc.cluster.local`, the local Linux resolver sends a DNS query over UDP 53 to CoreDNS. CoreDNS responds with the Service ClusterIP VIP (`10.96.0.10`).\n4. **Kernel Datapath Packet Interception (`kube-proxy`)**: The client container initiates a TCP SYN packet addressed to `10.96.0.10:80`. As the packet traverses the host kernel networking stack:\n   - **iptables Mode**: Netfilter traverses the `KUBE-SERVICES` chain to `KUBE-SVC-XXX`, applying random probabilistic matching (`-m statistic --mode random --probability 0.33`) to select an endpoint, executing Destination NAT (DNAT) to rewrite the destination IP to the real Pod IP (`10.244.2.40:8080`).\n   - **IPVS Mode**: The kernel IP Virtual Server (IPVS) module performs direct hash-table lookups (`ipset`) in $O(1)$ time complexity, routing the packet to the selected backend.\n   - **eBPF Mode (Cilium)**: eBPF programs attached to the cgroup socket egress (`sockops`) rewrite the destination IP and port in the socket data structure before packets are even emitted to the network stack.\n5. **Direct CNI Pod Delivery**: The rewritten IP packet traverses the overlay or direct routing CNI network (Calico, Cilium, AWS VPC CNI) directly to the target Pod's `veth` interface, where the application process processes the request and responds.",
        "serialization_protocols": "- **DNS RFC 1035 UDP/TCP Wire Protocol**: CoreDNS processes binary DNS requests over port 53, caching A, AAAA, and SRV record lookups in-memory.\n- **Netlink System Calls**: `kube-proxy` in IPVS mode uses Linux Netlink sockets (`AF_NETLINK`) to program kernel IPVS virtual servers and `ipset` collections directly without spawning shell processes.\n- **Protobuf API Streaming**: The EndpointSlice controller streams high-scale endpoint updates over Protobuf connections to avoid JSON serialization bottlenecks in clusters with tens of thousands of pods.",
        "components": "- **CoreDNS**: High-performance, plugin-based DNS server running in `kube-system`, dynamically configured via Kubernetes API watches to serve cluster-internal service discovery.\n- **kube-proxy**: Node daemon responsible for reflecting Kubernetes Service definitions into Linux kernel Netfilter (`iptables`), IPVS, or user-space routing tables.\n- **EndpointSlice**: Scalable replacement for legacy `v1.Endpoints`, splitting large endpoint lists into chunks of up to 100 endpoints to minimize network traffic and API server serialization latency on large deployments.\n- **Linux Netfilter & Conntrack**: Kernel connection tracking subsystem that stores active bidirectional TCP/UDP flow state in `/proc/net/nf_conntrack`, enabling accurate reverse SNAT/DNAT on reply packets.",
        "mechanics_failures": "- **Conntrack Table Saturation**: High connection churn without TCP connection reuse can exceed `nf_conntrack_max`, causing the kernel to drop new TCP SYN packets silently with `nf_conntrack: table full, dropping packet`.\n- **Stale Endpoints During Rolling Updates**: If a terminating Pod does not immediately unregister from the EndpointSlice before shutting down its HTTP server, incoming in-flight TCP requests receive `TCP RST` (Connection Refused) errors. Implementing a `preStop` hook (`sleep 5`) gives the EndpointSlice controller time to propagate removal across all nodes.\n- **CoreDNS 5-Second DNS Lookup Delays**: Linux `glibc` sends IPv4 (A) and IPv6 (AAAA) DNS queries concurrently over UDP. Netfilter conntrack race conditions can cause one query to be dropped, triggering a full 5-second glibc DNS retry timeout (mitigated by configuring `single-request-reopen` in `/etc/resolv.conf` or deploying NodeLocal DNSCache).",
    },
    6: {
        "flow": "1. **Public DNS Resolution**: An external internet client queries public DNS for `api.example.com`. DNS resolves to the public IPv4/IPv6 Elastic IP of the cloud infrastructure Load Balancer (AWS NLB, GCP Cloud LB, or Azure LB).\n2. **Layer 4 Load Balancer Distribution**: The Cloud Load Balancer terminates or passes through TCP/TLS connections and distributes raw Layer 4 traffic across the cluster worker nodes hosting the Ingress Controller daemon.\n3. **Ingress Controller Proxying**: The Ingress Controller (Envoy Proxy, NGINX, or Traefik) receives the TCP stream. It performs TLS termination using the X.509 certificate stored in a Kubernetes `Secret` (`tls.crt`/`tls.key`).\n4. **Dynamic Routing Table Evaluation**: The proxy inspects the HTTP/1.1 `Host` header or HTTP/2 `:authority` pseudo-header and the URL path (e.g. `/v1/checkout`). It matches these against the compiled Ingress rules (`spec.rules[*].http.paths`).\n5. **Direct Pod Reverse-Proxying**: The Ingress Controller bypasses intermediate `kube-proxy` ClusterIP hops by directly querying `EndpointSlice` IPs. It opens an upstream HTTP keep-alive connection directly to the selected application Pod (`10.244.2.40:8080`), streaming the HTTP request and piping the response back to the client.",
        "serialization_protocols": "- **Envoy Dynamic xDS gRPC APIs**: Modern ingress controllers use Envoy's Discovery Services (`LDS` for Listeners, `RDS` for Routes, `CDS` for Clusters, `EDS` for Endpoints) streaming Protobuf payloads over bidirectional gRPC over HTTP/2.\n- **HTTP/1.1, HTTP/2 & gRPC Upstream Wire Protocols**: The ingress edge terminates external client protocols and negotiates ALPN (`h2`, `http/1.1`), forwarding requests upstream over persistent TCP connection pools.\n- **OpenSSL / BoringSSL TLS Handshake**: Server Name Indication (SNI) routing and TLS certificate verification executed during client cryptographic session establishment.",
        "components": "- **Cloud Load Balancer (NLB/ALB)**: Layer 4 high-throughput load balancer provisioned by the cloud controller manager in response to `Service.spec.type: LoadBalancer`.\n- **Ingress Controller**: Software reverse proxy running inside the cluster that continuously reconciles `networking.k8s.io/v1 Ingress` resources into low-level proxy configuration files or in-memory xDS trees.\n- **TLS Secret Subsystem**: PEM-encoded X.509 certificate chains and RSA/ECDSA private keys injected into proxy memory for dynamic SNI certificate matching.\n- **Direct Upstream Endpoint Pool**: In-memory proxy routing table maintained by subscribing to Kubernetes EndpointSlice changes to enable sub-millisecond route convergence.",
        "mechanics_failures": "- **NGINX Reload Latency Spikes**: Legacy Ingress controllers write text `nginx.conf` files and trigger `nginx -s reload` on every endpoint change. In high-churn clusters, frequent reloads exhaust worker process file descriptors and drop active client TCP connections.\n- **Path Precedence Misconfigurations**: Ingress paths default to `pathType: Prefix`. If a generic catch-all path (`/`) is evaluated ahead of a specific subpath (`/v1/checkout`), traffic may route to unintended default backends unless exact ordering and longest-prefix-match rules are applied.\n- **Client IP Masking (SNAT)**: When external traffic routes through intermediate NodePort hops, the node executes Source NAT (SNAT), replacing the client's public IP with the node's internal IP. Setting `service.spec.externalTrafficPolicy: Local` preserves the original client IP and avoids unnecessary cross-node hops.",
    },
    7: {
        "flow": '1. **Pod Enqueueing into Scheduling Queue**: When a Pod with `spec.nodeName == ""` is created, `kube-scheduler` places the Pod into its internal three-tiered scheduling queue: `ActiveQ` (ready for evaluation), `BackoffQ` (backoff timer after unschedulable attempt), or `UnschedulablePods` pool.\n2. **Phase 1: Filter (Predicates)**: The scheduler evaluates all candidate nodes in parallel across active filter plugins:\n   - `NodeResourcesFit`: Verifies that node allocatable CPU, memory, and ephemeral storage satisfy `spec.containers[*].resources.requests`.\n   - `NodeName` & `NodePorts`: Verifies explicit node pinning and host port availability.\n   - `NodeAffinity`: Evaluates `requiredDuringSchedulingIgnoredDuringExecution` label selectors.\n   - `TaintToleration`: Verifies that the Pod possesses tolerations for all node taints with `NoSchedule` or `NoExecute` effects.\n3. **Phase 2: Score (Priorities)**: Surviving nodes are ranked from 0 to 100 across scoring plugins:\n   - `NodeResourcesBalancedAllocation`: Scores nodes higher when CPU and memory allocations maintain proportional balance.\n   - `ImageLocality`: Awards bonus points to nodes that have already pulled the container image layers to minimize startup latency.\n   - `PodTopologySpread`: Penalizes nodes that would violate failure domain distribution rules across availability zones.\n4. **Phase 3: Reserve & Permit**: The scheduler selects the highest-scoring node and calls the `Reserve` plugin, locking memory/CPU allocations in local scheduler memory to prevent race conditions with concurrent scheduling threads. `Permit` plugins can pause binding (e.g. for gang scheduling).\n5. **Phase 4: PreBind & Bind**: The scheduler executes `PreBind` (e.g., dynamic network/storage claim attachment) and issues an asynchronous HTTP POST `Binding` request to `kube-apiserver`, setting `spec.nodeName` on the target Pod in `etcd`.',
        "serialization_protocols": '- **Scheduling Framework Go Plugin Interfaces**: Internal scheduler extensions execute in-process via Go interface function calls (`Filter()`, `Score()`, `Reserve()`, `Bind()`) with zero network serialization overhead.\n- **HTTP REST / JSON Binding Subresource**: The scheduler posts a `v1.Binding` object (`{ "target": { "name": "node-2" } }`) to `/api/v1/namespaces/{ns}/pods/{name}/binding` over TLS HTTP/2.\n- **Leader Election Lease Protocol**: Multiple scheduler replicas coordinate active leadership using `coordination.k8s.io/v1 Lease` objects with atomic renewal heartbeats.',
        "components": "- **kube-scheduler**: Centralized, multi-threaded control plane binary responsible for optimal placement of unscheduled workloads across cluster nodes.\n- **Scheduling Queue (ActiveQ & BackoffQ)**: Priority queue data structure sorting Pods by priority (`spec.priority`) and creation timestamp.\n- **Scheduling Framework**: Extensible architecture defining extension points (QueueSort, PreFilter, Filter, PostFilter, PreScore, Score, Reserve, Permit, PreBind, Bind, PostBind).\n- **Node Allocatable Subsystem**: Node capacity model subtracting system reservations (`kube-reserved`, `system-reserved`, `eviction-threshold`) from raw physical hardware capacity.",
        "mechanics_failures": "- **Deadlock via Unbounded Preemption**: When a high-priority Pod preempts lower-priority Pods to free capacity, frequent preemption churn can cause lower-priority workloads to thrash in infinite restart loops if cluster capacity remains structurally deficient.\n- **Scheduler Cache Desynchronization**: If the scheduler's local in-memory snapshot of node resource allocations falls out of sync with `kube-apiserver` events, it may attempt to bind Pods to nodes with insufficient capacity, triggering `UnexpectedAdmissionError` on the worker kubelet.\n- **Affinity Topology Calculation Bottlenecks**: Heavy utilization of `podAffinity` and `podAntiAffinity` requires $O(N \times M)$ cross-pod topology comparisons across every node, causing scheduling throughput to degrade significantly on large clusters (>1,000 nodes).",
    },
    8: {
        "flow": "1. **Authentication (AuthN)**: A client initiates an HTTPS request to `kube-apiserver`. The server passes the request through configured authentication handlers in sequence:\n   - **X.509 Client Certificates**: Validates client certificate signatures against the cluster CA; extracts the Common Name (`CN`) as the Username and Organizational Units (`O`) as Groups.\n   - **OIDC Bearer Tokens**: Validates JSON Web Tokens (JWT) signed by an external identity provider (Okta, Keycloak, Dex) via public JWKS keys.\n   - **ServiceAccount Tokens**: Validates signed JWT service account tokens (`sub: system:serviceaccount:namespace:sa-name`) using RSA/ECDSA public keys.\n2. **Authorization (AuthZ)**: The authenticated subject (`User`, `Group`, or `ServiceAccount`) is evaluated by the RBAC Authorization Engine. The engine performs an in-memory lookup across:\n   - `Roles` & `RoleBindings` (scoped to specific namespaces).\n   - `ClusterRoles` & `ClusterRoleBindings` (cluster-wide or across all namespaces).\n   The engine matches the request API Group, Resource, Subresource, and Verb (`get`, `list`, `watch`, `create`, `delete`). If any rule matches, authorization is granted; if no rules match, the request is rejected with `403 Forbidden`.\n3. **Admission Control & Security Standards**: Authorized requests pass through Mutating and Validating Admission Webhooks and built-in Admission Controllers, including the **Pod Security Standards** engine (Privileged, Baseline, Restricted).\n4. **etcd Transaction**: If all validation and policy checks pass, the API server commits the state change to `etcd`.",
        "serialization_protocols": "- **JWT (JSON Web Signature RFC 7515)**: Projected ServiceAccount tokens and OIDC tokens are base64url-encoded signed JSON payloads containing standard claims (`iss`, `sub`, `aud`, `exp`, `nbf`).\n- **SubjectAccessReview JSON/Protobuf API**: Kubernetes API enabling external proxies to delegate authorization decisions by posting `authorization.k8s.io/v1 SubjectAccessReview` payloads to the API server.\n- **X.509 ASN.1 DER Encoding**: TLS transport client certificates encoded in standard ASN.1 Distinguished Encoding Rules format.",
        "components": "- **AuthN Authenticator Pipeline**: Chain of pluggable Go authenticators (x509, Webhook, OIDC, TokenFile, ServiceAccountToken).\n- **RBAC Authorizer**: In-memory rule index indexing role rules by verb and resource group for sub-microsecond permission checks.\n- **Pod Security Admission (PSA)**: Built-in admission controller replacing legacy PSPs, evaluating Pod specifications against `privileged`, `baseline`, and `restricted` security profiles enforced via namespace labels.\n- **Projected Token Volume Driver**: Kubelet driver responsible for refreshing and rotating signed ServiceAccount JWT tokens mounted into `/var/run/secrets/kubernetes.io/serviceaccount/token` before expiration.",
        "mechanics_failures": '- **Silent Token Expiration in Long-Running Daemons**: Legacy applications that cache the ServiceAccount token in memory on startup will encounter `401 Unauthorized` errors after the projected token rotates on disk (default 1-hour expiration) unless they re-read `/var/run/secrets/kubernetes.io/serviceaccount/token` dynamically.\n- **Privilege Escalation Prevention**: The RBAC engine blocks users from creating or updating Roles with permissions they do not personally possess, preventing unauthorized privilege escalation unless the user holds the explicit `escalate` verb.\n- **Over-Permissioned Wildcard Roles (`*`)**: Granting `apiGroups: ["*"]` and `resources: ["*"]` with `verbs: ["*"]` violates least privilege and exposes the entire cluster control plane to compromise if a single workload service account is leaked.',
    },
    9: {
        "flow": "1. **NetworkPolicy Resource Submission**: An operator applies a `networking.k8s.io/v1 NetworkPolicy` targeting a specific set of Pods via `spec.podSelector: { matchLabels: { app: database } }`.\n2. **Default-Deny Isolation Enforcement**: The declaration of `policyTypes: [Ingress, Egress]` immediately transitions all selected Pods into an isolated state. All traffic not explicitly allowed by the policy rules is dropped by the kernel.\n3. **CNI Agent Rule Compilation**: The CNI daemon running on each worker node (Cilium, Calico, or kube-router) watches NetworkPolicy resources:\n   - **eBPF Mode (Cilium)**: Compiles the policy rules into BPF bytecode, updating in-kernel BPF maps (`cilium_policy`) attached to the Pod's network interfaces.\n   - **iptables Mode (Calico)**: Writes targeted Netfilter chains (`cali-pi-*` for ingress, `cali-po-*` for egress) with ipset match sets.\n4. **Ingress Rule Evaluation**: Incoming packets destined for the database Pod are inspected:\n   - **Allowed Traffic**: Packets originating from source Pods matching `podSelector: { app: frontend }` on TCP port `5432` match the allow rule and are forwarded directly to the container socket.\n   - **Blocked / Dropped Traffic**: Packets originating from untrusted Pods or external IP ranges not matching explicit CIDR whitelists (`ipBlock`) fail all allow rules and are immediately dropped at the kernel socket or tc layer without returning TCP RST.",
        "serialization_protocols": "- **eBPF Map Binary Data Structures**: Network policy endpoints and CIDRs are written into in-kernel hash maps (`BPF_MAP_TYPE_HASH`, `BPF_MAP_TYPE_LPM_TRIE`) for $O(1)$ prefix matching in the kernel.\n- **Netlink / ipset Control Plane**: Calico and iptables-based CNIs serialize IP sets and port bitmasks over Linux Netlink sockets into kernel memory tables.\n- **Protobuf API Watch**: CNI node daemons stream NetworkPolicy, Pod, and Namespace state from `kube-apiserver` using HTTP/2 Protobuf connections.",
        "components": "- **CNI Node Agent (Cilium Agent / Calico Felix)**: Privileged host daemon that translates high-level Kubernetes label-based network policies into hardware or kernel-level packet filter rules.\n- **Linux TC (Traffic Control) & XDP Hooks**: eBPF attachment points at the network driver (XDP) or kernel queuing discipline (TC) processing packets before they enter the TCP/IP stack.\n- **Linux Netfilter Conntrack**: Stateful connection tracking table that automatically allows return traffic for established TCP connections without requiring reciprocal egress rules.\n- **LPM (Longest Prefix Match) Trie**: In-kernel algorithmic data structure used for high-speed IP CIDR classification (`10.0.0.0/16`).",
        "mechanics_failures": "- **Missing CNI Plugin Enforcement**: Standard default Kubernetes networking (e.g. basic Flannel) does **not** enforce NetworkPolicies. NetworkPolicy objects are accepted by `kube-apiserver` without errors, but packets are routed freely with zero isolation unless an enforcement-capable CNI (Cilium, Calico) is installed.\n- **Loopback & Host Network Egress Blocks**: An over-restrictive egress policy (`policyTypes: [Egress]` without DNS rules) will block access to CoreDNS (`10.96.0.10:53`), causing all internal service name resolutions to fail with timeout errors.\n- **Cross-Namespace Selector Omission**: In `from.namespaceSelector`, omitting the namespace selector restricts matching strictly to Pods within the **same** namespace. To allow ingress from a frontend pod in another namespace, both `namespaceSelector` and `podSelector` must be defined concurrently.",
    },
    10: {
        "flow": '1. **Container Process Startup (PID 1)**: The container runtime launches the container process inside isolated namespaces.\n2. **Phase 1: Startup Probe Evaluation**:\n   - `kubelet` initiates the configured `startupProbe` (HTTP GET, TCP Socket, gRPC, or Exec).\n   - All `livenessProbe` and `readinessProbe` checks remain **disabled** while the startup probe executes.\n   - If the startup probe succeeds within its `failureThreshold * periodSeconds` budget, the container transitions to active operational monitoring. If the budget is exhausted without success, `kubelet` kills the container and initiates a restart.\n3. **Phase 2: Operational Monitoring (Liveness & Readiness Parallel Loops)**:\n   - **Liveness Probe**: `kubelet` periodically executes the liveness check (e.g., HTTP `/healthz`). If consecutive failures reach `failureThreshold`, `kubelet` terminates the container process with `SIGTERM` (followed by `SIGKILL` after `terminationGracePeriodSeconds`) and increments the restart counter.\n   - **Readiness Probe**: `kubelet` periodically executes the readiness check (e.g., HTTP `/ready`).\n     - **Success**: `kubelet` sets `.status.conditions[Type=Ready].status = "True"`. The `EndpointSlice` controller includes the Pod IP, enabling it to receive traffic from Kubernetes Services.\n     - **Failure**: `kubelet` sets `Ready = False`. The `EndpointSlice` controller immediately removes the Pod IP from active endpoint pools, diverting traffic away from the unready container without killing the process.',
        "serialization_protocols": "- **HTTP/1.1 & HTTP/2 Prober Client**: Kubelet's internal Go HTTP prober sends standard HTTP requests with configurable headers and timeouts directly to the container IP.\n- **gRPC Health Checking Protocol (v1)**: Kubelet invokes the standard `grpc.health.v1.Health/Check` RPC over HTTP/2, evaluating the returned `ServingStatus` enum (`SERVING`, `NOT_SERVING`).\n- **Linux `/bin/sh -c` Exec RPC**: For Exec probes, kubelet issues a `ExecSync` CRI gRPC call, evaluating the process exit code (`0` = Success, non-zero = Failure).",
        "components": "- **Kubelet Prober Worker**: Dedicated background goroutine pool inside kubelet executing non-blocking probe checks per container according to `periodSeconds` and `timeoutSeconds`.\n- **EndpointSlice Controller**: Kubernetes controller that synchronizes Pod `Ready` status conditions into active load balancer and service routing tables.\n- **Container Lifecycle Handler (`postStart` / `preStop`)**: Execution hooks run asynchronously (`postStart`) or blocking before SIGTERM (`preStop`) to enable graceful connection draining.\n- **Linux Process Signal Manager**: Kubelet subsystem dispatching `SIGTERM` (15) and `SIGKILL` (9) to container PID 1.",
        "mechanics_failures": '- **Cascading Failure Loop (Liveness on Overloaded Backends)**: Configuring a Liveness probe to check deep dependencies (like backend database connectivity) causes overloaded servers to fail liveness checks, prompting kubelet to kill and restart all instances simultaneously, amplifying the outage. Liveness probes must **only** verify shallow process health.\n- **Readiness Flapping under Heavy Load**: If `timeoutSeconds` is set too low (e.g., 1s) and the application experiences transient CPU starvation, readiness probes timeout, dropping the Pod from service endpoints and overloading the remaining healthy replicas.\n- **PID 1 Signal Swallowing**: If an application runs under a shell script without `exec` (e.g. `CMD ["sh", "-c", "node server.js"]`), the shell process becomes PID 1 and may swallow `SIGTERM`, preventing graceful connection shutdown until `terminationGracePeriodSeconds` expires and `SIGKILL` is issued.',
    },
    11: {
        "flow": "1. **Metrics Collection Pipeline**: `cAdvisor` (embedded inside `kubelet`) continuously collects Linux cgroup CPU and memory metrics, while external collectors (Prometheus, Datadog) scrape custom application metrics. `metrics-server` aggregates these and exposes them via the `metrics.k8s.io` subresource.\n2. **HPA Reconciler Evaluation**: The Horizontal Pod Autoscaler (HPA) controller inside `kube-controller-manager` queries the Metrics API on a periodic sync loop (default: every 15 seconds). It calculates the desired replica count using the standard formula:\n   $$\\text{DesiredReplicas} = \\left\\lceil \\text{CurrentReplicas} \\times \\left( \\frac{\\text{CurrentMetricValue}}{\\text{TargetMetricValue}} \\right) \\right\\rceil$$\n3. **Scale Subresource Mutation**: The HPA controller issues an HTTP `PATCH` request to the target Deployment or StatefulSet's `/scale` subresource, updating `spec.replicas`.\n4. **Surge Pod Creation & Pending Capacity**: The Deployment controller spins up new Pods. If the existing cluster worker nodes lack available CPU/memory allocatable capacity, the newly created Pods remain in `Pending` state with `PodReasonUnschedulable`.\n5. **Cluster Capacity Scaling (Karpenter / Cluster Autoscaler)**:\n   - **Karpenter**: Subscribes directly to unschedulable Pod events via API watch, determines the optimal instance type (e.g. AWS `c6i.2xlarge` or Spot instance) by evaluating Pod resource requests and topology spread constraints, and calls the cloud Fleet API to launch a right-sized node in under 45 seconds.\n   - **Cluster Autoscaler**: Identifies pending pods, maps them to existing Node Groups / Auto Scaling Groups (ASGs), and increments the ASG desired capacity count.",
        "serialization_protocols": "- **Prometheus OpenMetrics Exposition Format**: Metrics scraped by Prometheus use plain-text or Protobuf-encoded OpenMetrics schemas over HTTP/1.1.\n- **Custom Metrics API Aggregator**: The API server aggregates custom metric providers via the API Aggregator layer (`apiregistration.k8s.io/v1`), proxying requests over TLS HTTP/2 to Prometheus Adapter or KEDA metrics servers.\n- **Cloud Provider Fleet APIs**: Karpenter and Cluster Autoscaler communicate with cloud compute APIs (AWS EC2 `CreateFleet`, GCP Compute Engine API) using signed HTTPS JSON/REST calls.",
        "components": "- **cAdvisor (Container Advisor)**: Daemon embedded inside `kubelet` that reads raw Linux kernel cgroups (`/sys/fs/cgroup/cpu`, `/sys/fs/cgroup/memory`) to expose container resource telemetry.\n- **Horizontal Pod Autoscaler (HPA)**: Control loop calculating dynamic replica scaling based on CPU, memory, or custom Prometheus metrics with configurable stabilization windows.\n- **Vertical Pod Autoscaler (VPA)**: Controller that adjusts container `requests` and `limits` in-place or by recreating Pods to right-size long-term resource allocations.\n- **Karpenter / Cluster Autoscaler**: Infrastructure-level controllers that provision physical or virtual cloud worker instances in response to pending unschedulable workloads.",
        "mechanics_failures": "- **Autoscaling Thrashing & Flapping**: Rapid spikes in traffic can cause HPA to aggressively scale replicas up and immediately down, destabilizing backend systems. Configuring `behavior.scaleDown.stabilizationWindowSeconds: 300` enforces a cool-down buffer to prevent thrashing.\n- **Missing Resource Requests**: HPA utilization targets (e.g., `averageUtilization: 70%`) calculate percentage utilization strictly relative to `spec.containers[*].resources.requests`. If a container lacks resource requests, HPA cannot calculate the ratio and fails to scale.\n- **HPA and VPA Mutual Conflict**: Running HPA on CPU/memory targets concurrently with VPA on the same Deployment causes the two controllers to fight in a feedback loop (HPA scales replicas while VPA scales container sizes). Use VPA in `recommendationMode` or run HPA on custom application metrics (e.g. RPS) when combining both.",
    },
    12: {
        "flow": "1. **CRD Registration & OpenAPI Schema Ingestion**: An administrator submits a `CustomResourceDefinition` (CRD) object (e.g. `postgresqlclusters.database.example.com`). `kube-apiserver` registers the new REST endpoints (`/apis/database.example.com/v1/...`), validates the embedded OpenAPI v3 structural schema, and establishes storage mappings in `etcd`.\n2. **Custom Resource (CR) Ingestion**: A developer submits a Custom Resource instance (e.g., `kind: PostgreSQLCluster`, `spec: { replicas: 3, storageGB: 50 }`). The API server validates the CR against the CRD's OpenAPI schema, rejects any malformed types, and stores the object.\n3. **Operator Informer Watch & Queueing**: The Operator process (written in Go with `controller-runtime`, Python with `Kopf`, or Rust with `kube-rs`) runs inside the cluster. Its `SharedIndexInformer` receives an `ADDED` or `MODIFIED` watch event from `kube-apiserver` and enqueues the resource key into a `RateLimitingWorkQueue`.\n4. **Reconciliation Loop Execution**: The operator reconciles desired vs actual state:\n   - Queries the cluster for child resources owned by the CR (StatefulSets, Services, ConfigMaps, Secrets).\n   - If child resources are missing or drift from the CR spec, the operator issues API calls to create or update them.\n   - Executes out-of-band operational tasks (e.g., running `pg_basebackup` or initializing database replication).\n5. **Status & Condition Reporting**: The operator issues an HTTP `PATCH` to the CR's `/status` subresource, updating `.status.conditions` (e.g., `Type: Ready, Status: True, Message: \"Primary and 2 standbys active\"`) and increments `.status.observedGeneration`.",
        "serialization_protocols": "- **OpenAPI v3 JSON Schema Validation**: The API server enforces schema contracts defined in the CRD spec (`openAPIV3Schema`) before persisting custom resources.\n- **Server-Side Apply (SSA) YAML/JSON Protocol**: Modern operators use Server-Side Apply (`PATCH` with `Content-Type: application/apply-patch+yaml`), specifying `fieldManager: postgres-operator` to declare explicit field ownership and detect conflicting mutations.\n- **Dynamic Watch JSON Streams**: Custom resources are streamed over HTTP/2 chunked streams to client-go informers using JSON or Protobuf (for CRDs supporting Protobuf serialization).",
        "components": "- **CustomResourceDefinition (CRD)**: Kubernetes extension mechanism defining new resource types, versions, validation schemas, and subresources (`/status`, `/scale`).\n- **Operator / Custom Controller**: Domain-specific software agent packaging operational knowledge (backup, failover, upgrades, scaling) for complex stateful applications.\n- **OwnerReferences & Garbage Collection**: Child resources are stamped with `metadata.ownerReferences` pointing to the parent CR. When the parent CR is deleted, the Kubernetes Garbage Collector automatically deletes all child resources via cascading deletion.\n- **Finalizers (`metadata.finalizers`)**: Asynchronous pre-deletion hooks (e.g., `database.example.com/clean-cloud-disks`) that prevent immediate object deletion in `etcd` until the operator completes external teardown tasks and removes the finalizer string.",
        "mechanics_failures": "- **Deadlock on Stuck Finalizers**: If an operator crashes or is uninstalled while custom resources remain with active `metadata.finalizers`, attempts to delete the custom resources hang indefinitely in `Terminating` state until finalizers are manually patched out.\n- **Infinite Reconciliation Loops**: If an operator modifies a field in `.spec` during reconciliation (instead of `.status`), the modification triggers a new `MODIFIED` watch event, causing the operator to endlessly re-reconcile itself and saturate API server resources.\n- **CRD Version Conversion Webhook Timeouts**: When supporting multiple CRD API versions (`v1alpha1`, `v1`), version conversion webhooks must deserialize and translate schemas. If the conversion webhook service is unreachable, all API operations across older versions fail with `ConversionWebhookFailed`.",
    },
    13: {
        "flow": "1. **Failure Signal Detection**: An alert fires or a Pod fails to enter `Running` state. The engineer initiates diagnostic triage by querying `kubectl get pod -o wide` and inspecting the Pod phase and status conditions.\n2. **Phase 1: Pending Phase Diagnostics**:\n   - The Pod has not been assigned a node (`spec.nodeName` is empty).\n   - Check scheduler events via `kubectl describe pod <name>`.\n   - If `0/10 nodes available: Insufficient cpu/memory`: Cluster is out of allocatable capacity &rarr; Scale worker nodes or decrease container resource requests.\n   - If `0/10 nodes available: node(s) had untolerated taint`: Add matching toleration to `spec.tolerations` or fix node taints.\n3. **Phase 2: CrashLoopBackOff Diagnostics**:\n   - The container starts but exits repeatedly with a non-zero exit code. Kubelet applies exponential restart backoff (10s, 20s, 40s, up to 300s).\n   - Inspect previous container logs: `kubectl logs <pod> --previous`.\n   - Analyze container termination exit code in `kubectl describe pod <name>`:\n     - **Exit 137 (Fatal error signal 9 `SIGKILL`)**: The Linux kernel Out-Of-Memory (OOM) killer killed the container (`OOMKilled: true`). Increase `resources.limits.memory` or fix application memory leaks.\n     - **Exit 1 / 2**: Application runtime crash, uncaught exception, or missing configuration file.\n     - **Exit 127 / 128**: Container entrypoint binary not found or library dependency missing in image.\n4. **Phase 3: ImagePullBackOff Diagnostics**:\n   - Kubelet fails to download the container image.\n   - Verify image tag existence in the remote container registry.\n   - Verify registry authentication Secret (`imagePullSecrets`) and IAM repository permissions.\n5. **Phase 4: Running but No Traffic (Readiness Failure)**:\n   - The Pod is `Running` but receives zero HTTP traffic from its Service.\n   - Inspect `.status.conditions[Type=Ready]`. If `Ready: False`, the `readinessProbe` is failing &rarr; Inspect health check endpoint.\n   - If `Ready: True`, verify that `Service.spec.selector` labels match `Pod.metadata.labels` exactly, and confirm `kubectl get endpointslices` contains the Pod IP.",
        "serialization_protocols": "- **Linux Kernel Process Exit Codes**: Standard Unix exit status codes ($128 + \\text{Signal Number}$) returned via `waitpid()` system call to the container runtime and passed to kubelet over CRI gRPC.\n- **Kubelet `/logs` Streaming API**: Kubelet streams stdout/stderr log files directly from `/var/log/pods/` to the API server and `kubectl` over WebSocket or chunked HTTP/2 streams.\n- **Kubernetes Events API**: Transient warning and error events (`v1.Event`) are emitted by controllers and kubelet to `kube-apiserver` as structured JSON/Protobuf messages.",
        "components": "- **Kubelet PLEG (Pod Lifecycle Event Generator)**: Subsystem monitoring container lifecycle state changes via runtime relisting and Linux inotify events.\n- **Linux Kernel OOM Killer**: Low-memory subsystem terminating processes when physical RAM + swap is exhausted, prioritizing processes with highest `/proc/[pid]/oom_score`.\n- **ImagePullBackOff State Machine**: Kubelet backoff algorithm doubling pull retry intervals from 10s up to a maximum of 300s upon failed registry pulls.\n- **EndpointSlice Sync Loop**: Network control loop removing unready Pod IPs from service routing tables within milliseconds of readiness probe failure.",
        "mechanics_failures": "- **Silent OOMKilled without Logs**: When a process exceeds `limits.memory`, the Linux kernel dispatches an uncatchable `SIGKILL` (signal 9). The application cannot execute shutdown hooks or flush log buffers, leaving the container log file empty. Always verify `kubectl describe pod` for `OOMKilled: true` and `Exit Code: 137`.\n- **Ephemeral Storage Eviction**: If an application writes unbounded log files or temporary data to the root container filesystem or `emptyDir` volumes exceeding `limits.ephemeral-storage`, kubelet evicts the Pod with `Evicted: Pod ephemeral local storage usage exceeds limit`.\n- **Zombie / Stale DNS Caching**: Applications that resolve backend service DNS once at boot and cache the IP indefinitely will fail to connect when backend Pods are replaced during rolling updates. Ensure JVM/Node/Go DNS TTLs are configured to low values (e.g., 5-10s).",
    },
    14: {
        "flow": "1. **Git Commit as Single Source of Truth**: A developer pushes a Git commit updating declarative manifests (Helm, Kustomize, or raw YAML) in the repository's `main` branch.\n2. **GitOps Ingestion & Webhook Trigger**: The `argocd-repo-server` detects the commit via an immediate Git webhook (GitHub/GitLab) or during its standard 3-minute polling loop.\n3. **Manifest Rendering**: The `argocd-repo-server` executes `kustomize build` or `helm template` in a sandboxed environment to render complete, concrete Kubernetes YAML manifests representing the **Desired State**.\n4. **Live Cluster State Comparison**: The `argocd-application-controller` queries `kube-apiserver` for the **Live State** of resources in the target cluster, using local `SharedIndexInformer` caches.\n5. **Diff Evaluation & Sync Status**: The controller computes a three-way structural diff between the Desired State (Git), the Live State (Cluster), and the previously applied state. If drift is detected, the Application transitions to `OutOfSync`.\n6. **Reconciliation & Self-Healing (Auto-Sync)**: If `automated: { prune: true, selfHeal: true }` is enabled, the controller issues Server-Side Apply `PATCH` requests to `kube-apiserver` to reconcile drift. If unauthorized manual edits were made directly via `kubectl`, ArgoCD automatically overwrites them to enforce Git state.",
        "serialization_protocols": "- **Git Smart HTTP / SSH Protocol**: `argocd-repo-server` clones and fetches Git repositories over TLS-encrypted Git Smart HTTP or SSH public-key authentication.\n- **gRPC Inter-Service Communication**: ArgoCD microservices (`argocd-server`, `argocd-repo-server`, `argocd-application-controller`) communicate internally via high-throughput gRPC connections.\n- **Server-Side Apply (SSA) Patch Format**: Manifest synchronization utilizes YAML/JSON SSA requests (`application/apply-patch+yaml`) to manage fine-grained field ownership.",
        "components": '- **argocd-repo-server**: Stateless worker that clones Git repositories, generates manifests via Helm/Kustomize, and returns rendered YAML streams over gRPC.\n- **argocd-application-controller**: Core reconciliation daemon comparing desired vs live state, ordering resource sync waves, and enforcing self-healing policies.\n- **argocd-server**: API and UI server exposing REST and gRPC endpoints for web dashboard access, CLI commands, and RBAC enforcement.\n- **Sync Waves & Hooks**: Annotations (`argocd.argoproj.io/sync-wave: "1"`) dictating exact execution ordering (e.g. run database migration Job in Wave 0, deploy API in Wave 1).',
        "mechanics_failures": "- **CRD / Manifest Prune Destructive Deletion**: If a manifest is removed from Git and `prune: true` is enabled, ArgoCD deletes the resource from the live cluster. If a StatefulSet or CRD is pruned without orphan finalizers, underlying data stores may be permanently deleted.\n- **Repository Server OOM on Large Helm Charts**: Rendering large Helm charts or recursive Kustomize overlays in parallel can exhaust `argocd-repo-server` memory limits, causing sync timeouts and `rpc error: code = ResourceExhausted`.\n- **Live State Mutation Loops (Mutating Webhooks Conflict)**: If an in-cluster mutating webhook alters default fields (e.g. injecting sidecars or modifying replica counts) that are not declared in Git, ArgoCD marks the application perpetually `OutOfSync` unless `ignoreDifferences` rules are explicitly configured.",
    },
    15: {
        "flow": "1. **Cilium Agent Initialization & eBPF Program Loading**: The `cilium-agent` DaemonSet boots on every worker node, mounts the BPF virtual filesystem (`/sys/fs/bpf`), and compiles modular C programs into eBPF bytecode using LLVM/Clang.\n2. **Socket Layer Attachment (`sockops`)**: Cilium attaches eBPF programs to the Linux socket layer (`BPF_PROG_TYPE_SOCK_OPS`) and socket message filtering (`BPF_PROG_TYPE_SK_MSG`).\n3. **Short-Circuit Socket-to-Socket Datapath**: When Pod A initiates an HTTP request to Pod B on the same node:\n   - Cilium's `sockops` eBPF program intercepts the `tcp_connect` system call.\n   - It records the socket tuple in an in-kernel BPF map (`cilium_sock_ops`).\n   - The outbound packet payload is redirected **directly** from Pod A's socket buffer (`sk_buff`) to Pod B's socket buffer in kernel memory, completely bypassing the TCP/IP stack, iptables, and virtual ethernet device (`veth`) packet processing overhead.\n4. **Cross-Node Encryption (WireGuard / IPsec)**: If Pod B resides on a remote worker node, the kernel encapsulates the packet using in-kernel WireGuard or IPsec, encrypting the payload before transmission across the physical network.\n5. **Sidecarless L7 Proxying & Hubble Observability**: For Layer 7 policy enforcement (HTTP method/path filtering) or mTLS, Cilium redirects selected flows to an embedded, high-performance Envoy instance via eBPF. Hubble captures flow telemetry directly from kernel eBPF ring buffers without injecting sidecar containers.",
        "serialization_protocols": "- **eBPF Kernel Ring Buffers (`BPF_MAP_TYPE_RINGBUF`)**: Lockless, high-throughput memory-mapped ring buffers streaming network flow events from Linux kernel space to user-space Hubble daemons with zero memory copying.\n- **WireGuard Protocol (Noise Protocol Framework)**: Cryptographic UDP encapsulation operating over port 51871 utilizing Curve25519, ChaCha20-Poly1305, and BLAKE2s.\n- **Envoy Go Extension APIs**: High-speed IPC between Cilium C Go runtime and embedded Envoy proxies for L7 filtering.",
        "components": "- **cilium-agent**: Per-node daemon responsible for watching Kubernetes resources, compiling BPF programs, managing BPF maps, and driving local datapath routing.\n- **cilium-operator**: Cluster-wide controller managing IPAM (IP Address Management), CRD synchronization, and garbage collecting stale node allocations.\n- **Hubble**: Distributed networking and security observability platform providing L3/L4 and L7 real-time traffic flow visualization and metrics.\n- **eBPF Maps (`/sys/fs/bpf/tc/globals/`)**: In-kernel key-value data structures storing policy rules, connection tracking state, and endpoint routing tables.",
        "mechanics_failures": "- **Kernel Version Incompatibility**: Advanced eBPF features (like socket layer redirection and BPF LSM hooks) require Linux kernel 5.10+ or 6.1+. Running on older enterprise kernels (e.g. RHEL 7 / Linux 4.x) forces Cilium into fallback modes with reduced performance.\n- **BPF Map Size Exhaustion**: High-scale clusters with millions of concurrent TCP flows can exceed default `bpf-ct-global-any-max` limits, causing new connections to fail with `BPF map full`.\n- **MTU Misconfiguration in Encapsulated Overlays**: When using VXLAN/Geneve overlay tunneling without jumbo frames, failing to reduce the interface MTU by the encapsulation header size (50 bytes for VXLAN) leads to packet fragmentation and silent TCP timeouts for large HTTP payloads.",
    },
    16: {
        "flow": "1. **API Admission Interception**: A user submits a workload creation request (`kubectl create -f pod.yaml`). `kube-apiserver` authenticates and authorizes the request, then directs the JSON representation of the resource to the Admission Control pipeline.\n2. **Phase 1: Mutate Phase (Kyverno / OPA Gatekeeper)**:\n   - The API server sends an `AdmissionReview` POST request over HTTPS to the policy engine webhook service.\n   - The policy engine evaluates mutation rules (e.g., automatically injecting `securityContext.runAsNonRoot = true` or adding cost-center labels).\n   - The engine returns a `JSONPatch` (RFC 6902) array back to the API server, which applies the mutations to the in-memory object.\n3. **Phase 2: Generate Phase**: The policy engine generates dependent resources (e.g., creating default `NetworkPolicy` and `ResourceQuota` objects whenever a new `Namespace` is created).\n4. **Phase 3: Validate Phase**:\n   - The API server submits the mutated object to the Validating Admission Webhook.\n   - The policy engine evaluates declarative validation rules (written in Kyverno YAML, OPA Rego, or Kubernetes CEL).\n   - If the object violates a policy set to `validationFailureAction: Enforce` (e.g., requesting `privileged: true`), the policy engine returns `allowed: false` with a descriptive error message. The API server terminates the request with `403 Forbidden`.\n5. **Phase 4: Cryptographic Image Verification**: Kyverno/Gatekeeper verifies that container image digests are cryptographically signed using Sigstore Cosign keys or keyless Rekor transparency logs before permitting execution.",
        "serialization_protocols": '- **AdmissionReview JSON Wire Protocol (v1)**: `kube-apiserver` exchanges structured JSON payloads containing `request` metadata (UserInfo, Kind, Operation, Object, OldObject) and evaluates returned `response` objects (`allowed`, `status`, `patch`).\n- **JSONPatch RFC 6902 Serialization**: Mutation patches are serialized as base64-encoded JSON arrays containing discrete patch operations (`{"op": "add", "path": "/spec/securityContext/runAsNonRoot", "value": true}`).\n- **Common Expression Language (CEL)**: High-speed, non-Turing complete expression evaluation executed in-process inside `kube-apiserver` with sub-millisecond evaluation latency.',
        "components": "- **ValidatingAdmissionPolicy**: Built-in Kubernetes native CEL-based admission engine executing in-process inside `kube-apiserver` without external webhook network hops.\n- **Kyverno / OPA Gatekeeper Webhook Servers**: High-availability pods running mTLS webhook servers that evaluate complex cluster-wide policies.\n- **Sigstore Cosign & Rekor**: Cryptographic signing framework and public transparency log verifying software supply chain provenance for container artifacts.\n- **Policy Mutation Engine**: JSON transformation pipeline applying sequential RFC 6902 patch sets to object representations before persistence.",
        "mechanics_failures": "- **Webhook Deadlock on API Server Startup**: If policy engine webhook pods are down and `failurePolicy: Fail` is configured on a webhook targeting core resources, `kube-apiserver` rejects all Pod creations—including the policy engine's own pods—causing a fatal cluster deadlock. Webhooks must use `namespaceSelector` to exempt `kube-system` and the policy engine's namespace.\n- **CEL Evaluation Resource Exhaustion**: Highly recursive or deeply nested CEL expressions evaluated across high-frequency API calls can exceed the API server's per-request CEL budget (100,000 cost units), causing admission to fail with `CostLimitExceeded`.\n- **Drift between Audit and Enforce Modes**: Running policies in `Audit` mode flags violations in logs but allows non-compliant resources to be deployed. Transitioning directly to `Enforce` mode without remediation will block operational updates to existing non-compliant workloads.",
    },
    17: {
        "flow": "1. **Virtual Control Plane Boot**: A tenant deploys a `vcluster` instance inside a single designated namespace (e.g., `tenant-a-prod`) on a shared physical host Kubernetes cluster. `vcluster` launches a lightweight control plane binary (k3s or pure Kubernetes API server + SQLite/etcd) within a single StatefulSet Pod.\n2. **Virtual API Interaction**: The tenant connects `kubectl` directly to the `vcluster` API endpoint. The tenant operates with full cluster-admin privileges inside their virtual control plane, freely creating CustomResourceDefinitions (CRDs), Namespaces, and ClusterRoles without impacting the host cluster or other tenants.\n3. **State Persistence in Virtual Storage**: Virtual objects (CRDs, Namespaces, RoleBindings) are persisted strictly inside the tenant's virtual SQLite/etcd database.\n4. **Syncer Resource Translation**: The `vcluster` Syncer daemon runs a bidirectional reconciliation loop:\n   - Watches virtual Pods and Services created in the virtual API server.\n   - Translates and creates corresponding physical Pods in the host cluster's `tenant-a-prod` namespace, rewriting names with a unique tenant prefix (e.g. `web-app-x-default-x-tenant-a`).\n   - Copies status changes, IP addresses, and container events from the physical host Pods back into the virtual control plane.\n5. **Host Physical Execution & Quota Enforcement**: The host cluster's `kube-scheduler` schedules the translated Pods onto shared physical worker nodes. Host `ResourceQuota`, `LimitRange`, and `NetworkPolicy` objects applied to `tenant-a-prod` strictly govern the aggregate compute and network boundaries of the tenant.",
        "serialization_protocols": "- **gRPC / SQLite Database Serialization**: k3s-backed vclusters use SQLite with Write-Ahead Logging (WAL) or kine (SQL translation for etcd v3 API), storing virtual state with minimal memory footprint.\n- **Bidirectional Kubernetes Client-Go Informer Sync**: The syncer maintains two distinct client-go informer sets (one connected to the virtual API, one to the host API) over HTTPS TLS 1.3 streams.\n- **Token Exchange & Impersonation**: The syncer uses Kubernetes API token authentication and service account impersonation to interact safely with the host control plane.",
        "components": "- **vcluster Control Plane Pod**: Lightweight single-pod control plane running the virtual API server, controller-manager, and storage engine.\n- **Syncer Process**: Intelligent proxy daemon translating low-level execution resources (Pods, Services, PVCs, ConfigMaps, Secrets) between virtual and host namespaces while keeping high-level abstractions (CRDs, Namespaces) isolated.\n- **Host Namespace Boundary**: Physical security envelope enforcing Linux cgroup resource limits, NetworkPolicies, and Pod Security Standards across all tenant pods.\n- **Host Worker Node Fleet**: Shared physical hardware executing container processes in isolated Linux namespaces.",
        "mechanics_failures": "- **Host ResourceQuota Exhaustion**: If a tenant creates virtual Pods that exceed the host namespace's `ResourceQuota`, the syncer fails to create physical pods on the host. The virtual Pod remains stuck in `Pending` state with syncer error events.\n- **StorageClass Mapping Mismatches**: If virtual PVCs reference StorageClasses that do not exist or are not mapped in the syncer configuration, dynamic volume provisioning on the host cluster fails.\n- **Syncer Desynchronization on Host Deletions**: If an administrator deletes a physical tenant pod directly on the host cluster via `kubectl`, the syncer detects the divergence and immediately recreates the physical pod to match the virtual desired state.",
    },
    18: {
        "flow": "1. **Client API Request**: A client sends an HTTP POST/PUT/DELETE request to `kube-apiserver`. The request is authenticated (AuthN) and authorized (AuthZ).\n2. **Phase 1: Mutating Admission Webhook Execution**:\n   - The API server queries `MutatingWebhookConfiguration` objects matching the resource rule selectors.\n   - Webhooks are executed in **sequential order** over HTTPS mTLS connections.\n   - Each webhook service processes the `AdmissionReview` request and returns a `JSONPatch` (RFC 6902) document.\n   - The API server applies the patch, modifying the in-memory object (e.g. injecting sidecar containers, adding default storage annotations).\n3. **Phase 2: Schema Validation & Immutability**: The mutated object is validated against the OpenAPI v3 schema. Any mutations that introduce invalid fields or violate schema constraints fail immediately.\n4. **Phase 3: Validating Admission Webhook Execution**:\n   - The API server queries `ValidatingWebhookConfiguration` objects.\n   - Webhooks are executed in **parallel** across all registered validating services to minimize latency.\n   - Each service evaluates business logic (e.g. verifying security context or checking resource quotas).\n   - If any validating webhook returns `allowed: false`, the entire API operation is aborted, and the failure message is returned to the client.\n5. **Persistence in etcd**: If all mutating and validating hooks pass, the API server executes the atomic write transaction to `etcd`.",
        "serialization_protocols": "- **AdmissionReview v1 JSON Envelope**: HTTPS POST payload containing `request.uid`, `request.object`, `request.oldObject`, `request.userInfo`, and `request.dryRun`.\n- **JSONPatch RFC 6902 Protocol**: Mutating webhooks return a base64-encoded array of JSONPatch operations (`add`, `remove`, `replace`) executed sequentially on the target object document.\n- **mTLS with CA Bundle Verification**: `kube-apiserver` verifies the webhook server's TLS certificate against the PEM-encoded `caBundle` specified in the webhook configuration.",
        "components": "- **kube-apiserver Admission Controller Manager**: Internal pipeline orchestrating Mutating and Validating webhook phases with configurable timeouts and failure policies.\n- **Webhook Server Daemon**: Out-of-tree HTTPS microservice running inside or outside the cluster handling `AdmissionReview` JSON RPC requests.\n- **CABundle Trust Store**: Base64-encoded X.509 Certificate Authority string embedded directly into the webhook configuration to establish mutual TLS trust.\n- **Webhook Rule Selectors**: Object, namespace (`namespaceSelector`), and object label (`objectSelector`) filters determining which API operations trigger webhook invocations.",
        "mechanics_failures": "- **Cluster Lockout via `failurePolicy: Fail`**: If a webhook server becomes unreachable (due to node crash, DNS failure, or certificate expiration) and `failurePolicy: Fail` is configured, all matching API requests fail with `Internal error: failed calling webhook`.\n- **Webhook Request Timeout Degradation**: Default webhook timeouts (10s) can cause client `kubectl` requests to hang and timeout if webhook pods experience high latency under load. Webhook timeouts should be tuned to $\\le 3$ seconds.\n- **Infinite Mutation Cycles**: If two mutating webhooks make conflicting modifications to the same field, the API server reinvokes webhooks up to a maximum limit (default: 8 reinvocations) before aborting with `mutation recursion limit exceeded`.",
    },
    19: {
        "flow": "1. **Chart Loading & Values Resolution**: The developer runs `helm upgrade --install my-app ./chart --values prod-values.yaml`. The Helm CLI client reads the `Chart.yaml` metadata, loads default values from `values.yaml`, and merges override values according to strict precedence rules (Default `values.yaml` $\\rightarrow$ Parent Chart Values $\\rightarrow$ `--values` files $\\rightarrow$ `--set` CLI flags).\n2. **Go Template Rendering**: The Helm client executes the internal Go template engine augmented with Sprig functions (cryptographic hashing, string manipulation, dictionary lookups). It renders all template files in `templates/*.yaml` into concrete, valid Kubernetes YAML manifests.\n3. **Dry-Run & OpenAPI Validation**: If `--dry-run` or client-side validation is active, Helm validates the rendered manifests against the Kubernetes OpenAPI v3 schema via `kube-apiserver`.\n4. **Release Manifest Transmission**: Helm calculates the three-way merge patch between the currently deployed release, the new desired manifests, and live cluster state. It issues HTTP/2 REST requests to `kube-apiserver` to create, update, or delete workloads.\n5. **Release History Persistence in Secrets**: Helm compresses the complete release metadata (including raw chart, values, rendered manifest, and release status) using Gzip, encodes it in Base64, and writes a Kubernetes Secret named `sh.helm.release.v1.<release-name>.v<revision>` in the release namespace.",
        "serialization_protocols": "- **Go Template Engine (`text/template`)**: In-memory template evaluation engine executing control structures (`if/else`, `range`, `with`) and pipeline functions.\n- **YAML / JSON Encoding & Decoding**: Chart structures and values files parsed via `gopkg.in/yaml.v3`.\n- **Gzip Compressed Base64 Storage**: Release history records compressed via Gzip and stored within `v1.Secret` payloads under `data.release` to stay well within etcd's 1.5MB key size boundary.",
        "components": "- **Helm CLI Binary**: Client-side Go application responsible for chart dependency management, template rendering, and release lifecycle orchestration.\n- **Sprig Function Library**: Collection of over 100 template functions (regex, crypto, date formatting, list operations) embedded in Helm's rendering engine.\n- **Release Secret Subsystem**: Versioned Kubernetes Secrets tracking immutable release history, enabling atomic rollbacks (`helm rollback <release> <revision>`).\n- **Helm Lifecycle Hooks**: Annotations (`helm.sh/hook: pre-install, post-upgrade`) orchestrating pre-deployment database migrations or post-deployment integration tests.",
        "mechanics_failures": "- **Release State Lockout (`Pending-Install` / `Pending-Upgrade`)**: If a Helm deployment is interrupted or times out while waiting for pods to become ready, the release status remains locked in `pending-upgrade`. Subsequent Helm commands fail with `another operation (install/upgrade/rollback) is in progress`. Resolving this requires manually deleting the pending release secret.\n- **Type Coercion Template Rendering Crashes**: YAML values parsed without explicit type definitions can cause Go template runtime nil-pointer exceptions (e.g., evaluating `{{ .Values.service.port | int }}` when `port` is missing or passed as a string).\n- **Orphaned Resources on Unmanaged Manifest Changes**: Resources created outside Helm's template tree (or resources whose template filenames changed without proper ownership labels) will not be tracked or pruned by Helm during upgrades, resulting in resource leaks.",
    },
    20: {
        "flow": "1. **Base Configuration Parsing**: Kustomize reads `base/kustomization.yaml` and loads the declarative source manifests (`deployment.yaml`, `service.yaml`). The base represents DRY, reusable, environment-agnostic infrastructure definitions.\n2. **Overlay Target Selection**: The developer or CI pipeline targets a specific environment overlay directory (e.g. `kustomize build overlays/production`).\n3. **Strategic Merge & JSON 6902 Patch Evaluation**: Kustomize evaluates the overlay's `kustomization.yaml`:\n   - **Strategic Merge Patches**: Merges partial YAML snippets based on Kubernetes struct tags (e.g., updating `spec.replicas: 10` or modifying container memory limits while preserving all other base fields).\n   - **JSON 6902 Patches**: Applies precise RFC 6902 JSON mutation operations (`add`, `replace`, `remove`) targeting specific array indices or nested keys.\n4. **Cross-Cutting Transformer Execution**:\n   - `namePrefix` & `nameSuffix`: Prepends/appends environment identifiers (e.g., `prod-web-service`).\n   - `commonLabels` & `commonAnnotations`: Injects standardized telemetry, ownership, and Git commit metadata across all output resources and selectors.\n   - `namespace`: Overrides target namespaces across all resources.\n5. **ConfigMap & Secret Hash Generation**: For resources declared under `configMapGenerator` or `secretGenerator`, Kustomize appends a deterministic content-hash suffix to the resource name (e.g. `app-config-8f9h2k4m5b`). It automatically updates all `Pod.spec.volumes[*].configMap.name` references to match, triggering zero-downtime rolling updates whenever configuration changes.",
        "serialization_protocols": "- **Strategic Merge Patch (SMP) Algorithm**: Kubernetes-aware YAML patching engine that utilizes Go struct tags (`patchStrategy: merge`, `patchMergeKey: name`) to intelligently merge slices of objects (like container lists) by key instead of replacing the entire array.\n- **JSON 6902 Patch Protocol**: Standardized JSON patch specifications executed in-memory against YAML node trees.\n- **SHA-256 Content Hashing**: Cryptographic hash calculation executed on serialized ConfigMap/Secret key-value pairs to generate immutable 10-character resource suffixes.",
        "components": "- **Kustomize Engine**: Pure declarative configuration engine built into `kubectl` (`kubectl apply -k .`) and available as a standalone CLI.\n- **Resource Accumulator**: In-memory tree structure tracking all loaded resources, overlays, components, and transformers.\n- **ConfigMapGenerator / SecretGenerator**: Built-in generators creating versioned, immutable configuration objects with automatic reference rewriting.\n- **Kustomize Transformers**: Pipeline plugins executing cross-cutting modifications (Labels, Annotations, Namespaces, NamePrefix, Images).",
        "mechanics_failures": "- **Strategic Merge vs JSON Patch Collisions**: Custom Resource Definitions (CRDs) lack native Kubernetes Go struct merge tags; applying a Strategic Merge Patch to a CRD array replaces the entire array instead of merging items. CRD modifications must use explicit JSON 6902 patches.\n- **Stale ConfigMap Garbage Accumulation**: Generating hash-suffixed ConfigMaps on every commit creates new ConfigMap objects on the cluster. Unless automated pruning (via ArgoCD or `kubectl apply --prune`) is configured, obsolete ConfigMaps accumulate indefinitely in `etcd`.\n- **Selector Immutability Violations with `commonLabels`**: Changing `commonLabels` in an overlay modifies `spec.selector.matchLabels` on Deployments. Because Kubernetes Deployment selectors are immutable after creation, applying the modified overlay fails with `field is immutable` until the Deployment is deleted and recreated.",
    },
    21: {
        "flow": "1. **Role-Oriented Resource Separation**:\n   - **Infrastructure Admin**: Deploys the Gateway Controller (e.g., Envoy Gateway, Cilium Gateway, or GKE Gateway) and defines a `GatewayClass` (`gateway.networking.k8s.io/v1`) specifying the underlying proxy controller implementation.\n   - **Cluster Operator**: Defines a `Gateway` resource declaring physical listeners (e.g., port 80 HTTP, port 443 HTTPS with SNI TLS certificates) and allocated IP addresses.\n   - **Application Developer**: Authors isolated `HTTPRoute` or `GRPCRoute` objects attached to the Gateway via `spec.parentRefs`, defining fine-grained path matching, header routing, and traffic splitting rules.\n2. **Dynamic Control Plane Translation (Envoy xDS Streaming)**: The Gateway Controller watches `Gateway`, `GatewayClass`, and `HTTPRoute` resources via Kubernetes API informers. It compiles these declarative routing graphs into Envoy **xDS Discovery Services** (Listener Discovery `LDS`, Route Discovery `RDS`, Cluster Discovery `CDS`, and Endpoint Discovery `EDS`).\n3. **xDS gRPC Streaming**: The controller streams compiled xDS protobuf configurations over bidirectional HTTP/2 gRPC connections to the data plane Envoy proxy instances.\n4. **Client Request Routing**: An external client connects to the Gateway listener IP:\n   - Envoy performs TLS termination and matches the hostname (`store.example.com`).\n   - Evaluates `HTTPRoute` rules (e.g., `/cart` $\\rightarrow$ `cart-svc`).\n   - Executes weighted traffic splitting (e.g., 90% to stable `cart-svc`, 10% to canary `cart-canary-svc`).\n5. **Direct Upstream Stream**: Envoy opens an upstream HTTP/2 or HTTP/1.1 connection directly to the selected application Pod IP, streaming the request and returning the response.",
        "serialization_protocols": "- **Envoy Dynamic xDS (v3) gRPC Protocol**: Control plane streaming of `envoy.config.listener.v3`, `envoy.config.route.v3`, and `envoy.config.cluster.v3` protobuf payloads over HTTP/2.\n- **ALPN Protocol Negotiation**: Automatic negotiation of Application-Layer Protocol Negotiation (`h2`, `http/1.1`) during TLS handshake.\n- **Common Expression Language (CEL) Route Matching**: Gateway API uses CEL for complex header and query parameter matching rules evaluated in-memory.",
        "components": "- **GatewayClass**: Cluster-scoped template defining the controller implementation and default infrastructure parameters.\n- **Gateway**: Point of network traffic entry defining network endpoints, listener ports, TLS configurations, and allowed route namespaces (`allowedRoutes`).\n- **HTTPRoute / GRPCRoute**: Protocol-specific routing rules specifying match conditions (path, method, headers), filters (request redirect, header mutation), and backend service refs.\n- **Envoy Data Plane Proxy**: High-performance C++ proxy executing L7 routing, connection pooling, and circuit breaking.",
        "mechanics_failures": "- **Cross-Namespace Route Attachment Blocks**: If an `HTTPRoute` in namespace `team-a` references a `Gateway` in namespace `infra-gateway`, the route attachment is rejected unless the Gateway's `listeners[*].allowedRoutes.namespaces.from` is configured to `All` or `Selector`.\n- **Status Condition Degradation (`Accepted: False` / `Programmed: False`)**: Gateway API status conditions strictly indicate whether routes are syntactically accepted and successfully programmed into the proxy data plane. Inspect `kubectl describe httproute <name>` to diagnose unattached routes.\n- **Port Collision on Shared Gateways**: Two independent teams attempting to bind conflicting Hostname/Port combinations on the same Gateway without distinct SNI certificates will trigger validation errors, leaving one route unprogrammed.",
    },
    22: {
        "flow": "1. **Composite Resource Definition (XRD) Schema Registration**: A platform engineer defines a `CompositeResourceDefinition` (XRD) specifying an OpenAPI schema for an abstract, self-service infrastructure API (e.g., `kind: PostgreSQLInstance`).\n2. **Composition Pipeline Declaration**: The platform engineer authors a `Composition` resource defining how the abstract XRD translates into concrete cloud resources (e.g., an AWS RDS Instance + Security Group + Subnet Group).\n3. **Developer Self-Service Claim (XRC)**: An application developer creates a lightweight, namespace-scoped `CompositeResourceClaim` (XRC) requesting a database with high-level parameters (`storageGB: 50, tier: production`).\n4. **Crossplane Composition Engine Reconciliation**: The Crossplane core engine binds the XRC to a cluster-scoped `Composite Resource` (XR). The composition engine renders the configured pipeline, instantiating individual `Managed Resources` (MRs) (e.g. `RDSInstance.rds.aws.upbound.io`).\n5. **Provider Pod Cloud Actuation**: The provider pod (e.g., `provider-aws-rds`) detects the Managed Resource:\n   - Assumes an authorized cloud IAM role via Workload Identity / IRSA.\n   - Calls the cloud provider SDK (AWS Go SDK) to provision the real-world RDS database.\n   - Streams live cloud status, endpoints, and generated credentials back into a Kubernetes Secret in the developer's application namespace.",
        "serialization_protocols": "- **Cloud Provider REST / JSON SDKs**: Crossplane provider daemons communicate with cloud endpoints (AWS, GCP, Azure APIs) using signed HTTPS JSON/REST calls.\n- **OpenAPI v3 Schema Contracts**: XRDs validate composite claims against OpenAPI v3 structural schemas before composition pipelines execute.\n- **Kubernetes Secret Payload Streaming**: Connection details (host, port, username, password) are encrypted and stored in `v1.Secret` payloads.",
        "components": "- **CompositeResourceDefinition (XRD)**: Defines the custom API type and OpenAPI validation rules for platform abstractions.\n- **Composition**: Reusable blueprint mapping composite resources to underlying cloud Managed Resources (MRs) with patch-and-transform or pipeline functions.\n- **Crossplane Provider Pods**: Domain-specific controllers running in the cluster that reconcile Kubernetes MR objects against real cloud provider APIs.\n- **Connection Secret Subsystem**: Crossplane mechanism for dynamically capturing generated cloud credentials and writing them to designated tenant namespaces.",
        "mechanics_failures": "- **Cloud API Rate Limiting & Throttling**: Large-scale Crossplane deployments continuously reconciling hundreds of Managed Resources can exceed cloud provider API rate limits (e.g., AWS DescribeDBInstances throttling). Providers must be configured with exponential backoff and tuned polling intervals.\n- **Orphan vs Delete Policy Misconfiguration**: Managed Resources default to `spec.deletionPolicy: Delete`. Deleting an MR or its parent claim permanently deletes the physical cloud resource (e.g. terminating the RDS database). Setting `deletionPolicy: Orphan` preserves cloud infrastructure upon Kubernetes object deletion.\n- **Composition Patch Field Type Mismatches**: Type mismatches during Patch-and-Transform operations (e.g. attempting to patch an integer port into a string field) fail silently or produce reconciliation errors visible only in `kubectl describe composite`.",
    },
    23: {
        "flow": "1. **Tetragon Daemon Initialization**: The `tetragon` DaemonSet starts on every worker node, mounts the BPF virtual filesystem (`/sys/fs/bpf`), and loads its core eBPF sensor programs into the Linux kernel.\n2. **TracingPolicy CRD Ingestion**: Security engineers apply a `TracingPolicy` Custom Resource defining security invariants (e.g., monitoring file access to `/etc/shadow` or blocking raw socket creation in container namespaces).\n3. **In-Kernel eBPF Hook Attachment**: The Tetragon agent compiles the policy rules and attaches eBPF programs to targeted Linux kernel hook points:\n   - **Kprobes / Kretprobes**: Dynamic kernel function entry and exit tracing (e.g., `sys_execve`, `sys_read`).\n   - **Tracepoints**: Static, stable kernel execution points (e.g., `sys_enter_connect`).\n   - **BPF LSM (Linux Security Modules)**: Kernel security hooks (`security_file_open`, `security_socket_create`) that possess native authority to block system calls before execution.\n4. **Real-Time In-Kernel Enforcement (Synchronous SIGKILL)**: When a compromised process inside a container attempts an unauthorized action (e.g., reading `/etc/shadow`):\n   - The BPF LSM hook intercepts the system call in kernel space before the file read executes.\n   - If the policy specifies `action: Sigkill`, the eBPF program dispatches a kernel `SIGKILL` directly to the offending process PID, terminating it instantaneously with zero user-space latency.\n5. **High-Throughput Security Telemetry Streaming**: Tetragon streams structured JSON security audit events (containing PID, binary path, container ID, namespace, and user ID) from in-kernel lockless ring buffers directly to `/var/log/tetragon/events.json` and SIEM forwarders.",
        "serialization_protocols": "- **eBPF Ring Buffer (`BPF_MAP_TYPE_RINGBUF`)**: Lockless, high-speed memory-mapped circular buffers streaming kernel events to user-space Go agents with sub-microsecond latency.\n- **gRPC Event Stream (`tetragon.v1.FineGuidanceSensors`)**: Tetragon daemon streams real-time JSON/Protobuf security events to external SIEMs and `tetra` CLI clients over local Unix domain sockets.\n- **ELF (Executable and Linkable Format) Bytecode**: eBPF programs compiled via LLVM into ELF binaries and validated by the Linux in-kernel eBPF verifier.",
        "components": "- **Tetragon Agent**: User-space Go daemon managing BPF program lifecycles, compiling TracingPolicies, and streaming event telemetry.\n- **BPF LSM Subsystem**: Linux Security Module framework integrated with eBPF in Linux kernels $\\ge 5.7$, providing kernel-enforced preventive access control.\n- **In-Kernel Verifier**: Linux kernel safety checker verifying that eBPF programs terminate, avoid out-of-bounds memory access, and do not cause kernel panics.\n- **TracingPolicy CRD**: Declarative Kubernetes resource defining syscall hooks, filters, and enforcement actions.",
        "mechanics_failures": "- **Kernel Version Dependency for BPF LSM**: Preventive enforcement (`action: Sigkill` via LSM hooks) requires Linux kernel 5.10+ with `CONFIG_BPF_LSM=y` enabled in kernel boot parameters. On older kernels, Tetragon operates in observation-only mode without kill capabilities.\n- **High Syscall Volume Overhead**: Attaching tracing policies to extremely high-frequency syscalls (like `sys_read` or `sys_write` on busy database containers) without restrictive namespace filters can saturate CPU resources and fill kernel ring buffers.\n- **Ring Buffer Event Drops**: If user-space log consumers cannot process security events as fast as the kernel emits them, the ring buffer overflows, resulting in dropped event logs indicated by `events_lost` counters.",
    },
    24: {
        "flow": "1. **RayCluster CR Submission**: An ML platform engineer submits a `RayCluster` CR declaring head node configurations and dynamic worker node groups with GPU/TPU resource allocations.\n2. **KubeRay Controller Reconciliation**: The `kuberay-operator` detects the CR:\n   - Creates the **Ray Head Pod** hosting the Global Control Store (GCS), Ray API Server, Dashboard (port 8265), and Cluster Autoscaler.\n   - Creates a Kubernetes `Service` providing stable DNS endpoints to the Ray Head.\n   - Spawns the desired count of **Ray Worker Pods** across GPU-accelerated worker nodes.\n3. **Raylet Daemon & Plasma Store Startup**: Inside each Worker Pod:\n   - The `raylet` daemon starts and registers with the Ray Head's GCS via gRPC heartbeats over port 6379.\n   - Allocates a dedicated Linux shared memory segment (`/dev/shm`) backing the **Plasma Object Store** for ultra-fast, zero-copy deserialization of distributed NumPy/PyTorch arrays.\n4. **Distributed Task Scheduling & Execution**: A distributed training script connects to the Ray Head:\n   - Ray Head schedules tasks and actors across available Worker Pods based on GPU availability and data locality.\n   - Ray workers exchange intermediate tensor gradients directly using **NCCL (NVIDIA Collective Communications Library)** over high-speed RoCE/InfiniBand or VPC networks.\n5. **Dynamic Ray Cluster Autoscaling**: The embedded Ray Autoscaler monitors queue length and pending actor requests, issuing API calls to `kuberay-operator` to dynamically scale worker replica counts up or down.",
        "serialization_protocols": "- **Apache Arrow Plasma Shared Memory**: Zero-copy in-memory object store utilizing Apache Arrow binary IPC format for direct tensor memory sharing between co-located worker processes.\n- **gRPC over TCP / HTTP/2**: Inter-node control messaging between `raylet` daemons and Ray Head Global Control Store (GCS).\n- **NCCL Direct Peer-to-Peer Protocol**: Low-latency GPU-to-GPU memory exchange bypassing host CPU and OS networking stack via NVIDIA NVLink or GPUDirect RDMA.",
        "components": "- **kuberay-operator**: Kubernetes controller managing the complete lifecycle of Ray clusters, RayJobs, and RayServices.\n- **Ray Head Pod**: Central coordinator running the Global Control Store (GCS metadata), task scheduler, autoscaler, and monitoring dashboard.\n- **Ray Worker Pods**: Scalable execution fleet running user ML code, Python workers, and local Plasma shared memory stores.\n- **NCCL / NVLink Mesh**: High-bandwidth inter-GPU interconnect topology enabling multi-node distributed training (PyTorch DDP, DeepSpeed, Megatron-LM).",
        "mechanics_failures": "- **`/dev/shm` Size Starvation**: Docker and Kubernetes default `/dev/shm` to 64MB unless an `emptyDir` with `medium: Memory` is explicitly mounted. Plasma Object Store startup will crash with `Out of memory: Plasma store failed to allocate memory`.\n- **NCCL Ring Communication Timeout**: If firewall rules or NetworkPolicies block high-port TCP/RDMA traffic between worker pods, distributed training hangs indefinitely during tensor synchronization before failing with `NCCL WARN: Call to connect returned Connection timed out`.\n- **GCS Redis Heartbeat Loss**: Heavy CPU contention on the Ray Head node can delay heartbeat processing, prompting the head to falsely declare healthy worker nodes dead and terminating in-flight training jobs.",
    },
    25: {
        "flow": "1. **Batch Job Submission**: An ML engineer submits multi-pod batch workloads (e.g. PyTorch distributed jobs requiring 16 pods concurrently) referencing a `kueue.x-k8s.io/queue-name: ml-team-queue`.\n2. **Kueue Admission & Quota Evaluation**: The `kueue-controller-manager` intercepts the Job:\n   - Matches the job against the target `ClusterQueue` and its associated `ResourceFlavors` (e.g., `nvidia-a100-gpu`).\n   - Evaluates real-time quota usage across multi-tenant cohorts, calculating nominal quotas and borrowing limits from idle shared quotas.\n   - If quota is available, Kueue **admits** the workload, transitioning `Workload.status.conditions[Admitted] = True`. If quota is exhausted, the workload is queued in priority order without creating pending pods.\n3. **Volcano Gang Scheduler Activation**: The admitted workload creates a `PodGroup` resource managed by the `volcano-scheduler`:\n   - Declares `minMember: 16` (the minimum number of pods required for the job to function).\n4. **Gang All-or-Nothing Scheduling Pipeline**:\n   - Volcano evaluates cluster nodes in a single atomic scheduling transaction.\n   - If all 16 pods can be placed simultaneously across eligible GPU nodes, Volcano binds all 16 pods in unison.\n   - If only 12 nodes are available, Volcano places **zero** pods, preventing resource deadlock where two competing jobs hold partial allocations and neither can proceed.\n5. **Node Placement & Plugin Execution**: Volcano executes scheduling plugins (Dominant Resource Fairness `drf`, `binpack`, and `gang`), packing pods densely onto worker nodes to minimize node fragmentation.",
        "serialization_protocols": "- **Kueue Workload CRD API**: Workload specifications, resource flavors, and cohort quota reservations serialized as JSON/Protobuf across Kubernetes API endpoints.\n- **Volcano PodGroup Protocol**: Gang scheduling synchronization protocol tracking `minMember` admission barriers in-memory within the custom scheduler binary.\n- **Prometheus Metric Telemetry**: Real-time export of queue depths, waiting times, and quota borrowing metrics.",
        "components": "- **Kueue Controller**: Job-level admission controller managing multi-tenant queues, cohort resource borrowing, and preemption policies.\n- **Volcano Custom Scheduler**: High-performance batch scheduler replacing `kube-scheduler` for gang scheduling, task topology awareness, and fair-share scheduling.\n- **ClusterQueue & LocalQueue**: Hierarchical queue abstractions separating cluster-wide administrative quota allocations from namespace-scoped developer queues.\n- **PodGroup CRD**: Logical grouping of co-dependent pods requiring all-or-nothing scheduling and coordinated failure handling.",
        "mechanics_failures": "- **Deadlock from Non-Gang Schedulers**: Using standard `kube-scheduler` on multi-node distributed training jobs can lead to deadlocks where Job A claims 8 GPUs on Node 1 and Job B claims 8 GPUs on Node 2, leaving both jobs stuck in pending states indefinitely.\n- **Kueue Workload Unmanaged Job Leaks**: If a batch job is deleted manually without deleting its corresponding Kueue `Workload` object, quota reservations may remain locked in the `ClusterQueue`, blocking subsequent jobs from admission.\n- **Cohort Borrowing Preemption Storms**: When an idle cohort reclaims borrowed quota, aggressive preemption can abruptly terminate dozens of running batch jobs. Setting `reclaimWithinCohort: LowerPriority` ensures only lower-priority borrowed workloads are preempted.",
    },
    26: {
        "flow": "1. **Workload Hardware Claim Specification**: An AI inference Pod declares a dynamic resource requirement via `spec.resourceClaims[*].resourceClaimTemplateName`, referencing a `ResourceClaimTemplate`.\n2. **CEL Hardware Device Filtering**: The `ResourceClaimTemplate` specifies structured device matching rules evaluated via **Common Expression Language (CEL)** (e.g., `device.memory >= 80Gi && device.mig == true`).\n3. **Dynamic Resource Allocation (DRA) Controller Binding**:\n   - The centralized DRA driver controller evaluates available hardware devices reported by node driver plugins.\n   - Matches the CEL constraints against candidate GPUs (e.g. NVIDIA H100 with NVLink).\n   - Allocates the specific physical device ID and generates a bound `ResourceClaim` object in the API server.\n4. **Node Preparation via CDI gRPC (`NodePrepareResources`)**:\n   - When the Pod is scheduled to the selected node, `kubelet` calls the node's NVIDIA DRA Driver Plugin via gRPC `NodePrepareResources`.\n   - The plugin provisions the hardware slice and writes a **Container Device Interface (CDI)** JSON/YAML specification to `/var/run/cdi/nvidia.yaml`.\n5. **Container Runtime CDI Injection**:\n   - The container runtime (`containerd` / `CRI-O`) reads the CDI specification.\n   - Injects the designated device nodes (`/dev/nvidia0`, `/dev/nvidiactl`), Linux IPC capabilities, and driver libraries directly into the container's OCI runtime specification without requiring privileged mode.\n6. **Direct Hardware Execution**: The containerized TensorRT / vLLM runtime executes directly against the physical GPU hardware via PCIe DMA and NVLink.",
        "serialization_protocols": "- **Container Device Interface (CDI v0.5.0+) Schema**: Standardized JSON/YAML device definition schema describing host device nodes, mount paths, environment variables, and Linux cgroup permissions.\n- **DRA Driver gRPC Protocol (`kubelet.v1alpha1.DRAPlugin`)**: High-speed gRPC interface between `kubelet` and out-of-tree hardware vendor plugins over local Unix domain sockets.\n- **Common Expression Language (CEL)**: In-process expression language compiling hardware selector queries with strict memory and execution bounds.",
        "components": "- **Dynamic Resource Allocation (DRA) Controller**: Kubernetes resource scheduler subsystem replacing coarse integer device counting with fine-grained, stateful hardware parameter allocation.\n- **Container Device Interface (CDI)**: Standardized specification allowing third-party device vendors to configure container runtimes without vendor-specific runtime plugins.\n- **NVIDIA DRA Driver Plugin**: Node DaemonSet responsible for GPU discovery, MIG (Multi-Instance GPU) slicing, and CDI manifest generation.\n- **ResourceClaim & ResourceClaimTemplate**: Kubernetes declarative APIs defining dynamic hardware requests and tracking allocated physical device bindings.",
        "mechanics_failures": "- **CDI File Permission / Injection Failures**: If the CDI specification file in `/var/run/cdi/` is corrupted or has incorrect filesystem permissions, the container runtime fails container creation with `CDI device injection failed`.\n- **MIG Partition Allocation Conflicts**: Requesting specific Multi-Instance GPU (MIG) slice profiles (e.g. `1g.10gb`) on a GPU already configured with conflicting slice geometries requires dynamic repartitioning, failing if active processes occupy other slices.\n- **Driver Version Mismatch between Host and Container**: Utilizing CUDA libraries in the container image that require a higher NVIDIA kernel driver version than the host node's installed driver causes initialization to fail with `CUDA driver version is insufficient for CUDA runtime version`.",
    },
    27: {
        "flow": "1. **EKS OIDC Provider & IAM Trust Establishment**: An AWS administrator creates an IAM OpenID Connect (OIDC) identity provider for the EKS cluster. An IAM Role is provisioned with a Trust Policy granting `sts:AssumeRoleWithWebIdentity` restricted to a specific Kubernetes ServiceAccount (`system:serviceaccount:default:my-app-sa`).\n2. **Projected Token Volume Injection (IRSA / EKS Pod Identity)**:\n   - When a Pod referencing `my-app-sa` is submitted, the **EKS Pod Mutating Webhook** intercepts the Pod creation.\n   - Injects an ephemeral, cryptographically signed OIDC JWT token into `/var/run/secrets/eks.amazonaws.com/serviceaccount/token`.\n   - Injects environment variables: `AWS_ROLE_ARN` and `AWS_WEB_IDENTITY_TOKEN_FILE`.\n3. **AWS STS Token Exchange**:\n   - The application AWS SDK inside the container reads the projected JWT token file.\n   - Calls the AWS Security Token Service (STS) API: `sts:AssumeRoleWithWebIdentity`.\n   - AWS STS verifies the token signature against the EKS OIDC discovery endpoint (`https://oidc.eks.amazonaws.com/id/...`).\n   - STS issues temporary AWS credentials (AccessKeyId, SecretAccessKey, SessionToken) with 1-hour validity directly to the SDK in memory.\n4. **AWS Load Balancer Controller IP-Mode Ingress Routing**:\n   - The AWS Load Balancer Controller watches `Ingress` resources annotated with `alb.ingress.kubernetes.io/target-type: ip`.\n   - Directly configures AWS Application Load Balancer (ALB) Target Groups with individual Pod private IP addresses allocated by the **AWS VPC CNI**, completely bypassing NodePort hops.\n5. **Node Autoscaling via Karpenter**:\n   - When pending pods emerge, Karpenter queries EC2 Fleet APIs, calculates the most cost-effective instance types (Graviton, Spot, x86), launches the EC2 instances, and registers them directly with the EKS cluster in under 45 seconds.",
        "serialization_protocols": "- **AWS Signature Version 4 (SigV4) Protocol**: Application SDKs and controllers sign all HTTPS REST requests to AWS APIs using HMAC-SHA256 cryptographic signatures.\n- **OIDC Discovery JSON & JWKS Keys**: EKS exposes public JSON Web Key Sets (JWKS) over HTTPS enabling AWS STS to verify cluster-signed JWT tokens.\n- **AWS VPC CNI IPAM IPC**: Local IP Address Management daemon (`aws-k8s-cni`) uses Unix domain socket IPC to communicate with kubelet and assign ENI secondary IP addresses directly to Pod network namespaces.",
        "components": "- **IAM Roles for Service Accounts (IRSA)**: Cryptographic identity federation mapping Kubernetes ServiceAccounts directly to AWS IAM Roles without static long-lived credentials.\n- **AWS Load Balancer Controller**: Out-of-tree controller managing AWS ALBs, NLBs, and Target Groups in response to Kubernetes Ingress and Service objects.\n- **AWS VPC CNI (`amazon-vpc-cni-k8s`)**: High-performance networking plugin allocating native AWS VPC IP addresses and Elastic Network Interfaces (ENIs) directly to Pods.\n- **Karpenter**: High-velocity node autoscaler communicating directly with AWS EC2 Fleet and Pricing APIs to provision optimal compute capacity.",
        "mechanics_failures": "- **OIDC Audience Mismatch (`sts:AssumeRoleWithWebIdentity` Fails)**: If the IAM Role Trust Policy specifies `aud: sts.amazonaws.com` but the projected token uses a custom audience, STS rejects the token exchange with `AccessDenied: An error occurred (InvalidIdentityToken)`.\n- **VPC Subnet IP Address Exhaustion**: In high-density clusters, the AWS VPC CNI assigns secondary IP addresses from the node's subnet. If the VPC subnet CIDR block runs out of available IPs, new Pods fail to launch with `FailedCreatePodSandBox: no IP addresses available in subnet`.\n- **Target Group Registration Lag**: If Pods become ready before the AWS Load Balancer Controller finishes registering their IPs in the ALB Target Group, early traffic receives HTTP 502 Bad Gateway errors. Configure a `readinessGate` (`target-health.alb.ingress.kubernetes.io`) on the Pod to prevent traffic routing until target registration health checks pass.",
    },
    28: {
        "flow": "1. **Workload Identity Binding**: A GCP administrator creates a Google Service Account (GSA) and binds it to a Kubernetes ServiceAccount (KSA) using IAM Role `roles/iam.workloadIdentityUser` with member `serviceAccount:PROJECT_ID.svc.id.goog[NAMESPACE/KSA_NAME]`.\n2. **Metadata Server Token Interception**:\n   - The application Pod runs inside GKE with `serviceAccountName: ksa-name`.\n   - The application Google Cloud SDK makes an HTTP GET request to the local Metadata Server at `http://169.254.169.254/computeMetadata/v1/instance/service-accounts/default/token`.\n   - The **GKE Metadata Server** (a DaemonSet intercepting `169.254.169.254`) intercepts the request, verifies the Pod's KSA identity, and contacts Google Cloud IAM to exchange the Kubernetes identity token for a short-lived GCP OAuth2 access token.\n3. **GKE Autopilot Workload Right-Sizing**:\n   - Workloads deployed to GKE Autopilot specify resource requests. Autopilot automatically adjusts CPU-to-memory ratios to conform to supported compute classes (`GeneralPurpose`, `Performance`, `Scale-Out`).\n   - Autopilot provisions and manages underlying GCE compute, applying kernel hardening, automatic OS patch upgrades, and managed node pools.\n4. **GKE Gateway API & Cloud Armor Edge Security**:\n   - The GKE Gateway Controller provisions a Google Cloud Application Load Balancer in response to a `Gateway` resource.\n   - A `GCPBackendPolicy` CR attaches **Google Cloud Armor** security policies to the backend service, enforcing Web Application Firewall (WAF) rules and DDoS protection at Google's global edge network before traffic enters the cluster.",
        "serialization_protocols": "- **Google Cloud REST / gRPC APIs**: Google SDKs communicate with GCP services (BigQuery, Cloud Storage, Secret Manager) using HTTP/2 Protobuf gRPC or TLS REST calls.\n- **OAuth 2.0 Bearer Token Protocol (RFC 6750)**: GKE Metadata Server returns JSON OAuth2 tokens (`access_token`, `expires_in`, `token_type: Bearer`) passed in the HTTP `Authorization` header.\n- **GCP Gateway Controller CRD APIs**: `GCPBackendPolicy` and `FrontendConfig` resources serialized as JSON/Protobuf objects configuring Google Cloud Serverless Network Endpoint Groups (NEGs).",
        "components": "- **GKE Workload Identity**: Secure identity bridge linking Kubernetes ServiceAccounts to Google Cloud IAM without storing service account keys in cluster Secrets.\n- **GKE Metadata Server**: Local node proxy intercepting metadata requests to dynamically synthesize GCP OAuth2 tokens.\n- **GKE Autopilot Control Plane**: Fully managed GKE mode where Google provisions, scales, and manages nodes, billing per-Pod resource requests.\n- **Google Cloud Armor**: Edge-level network security service providing DDoS mitigation, OWASP Top 10 WAF filtering, and geo-fencing.",
        "mechanics_failures": "- **Metadata Server Pod Initialization Hangs**: If Workload Identity is enabled on the cluster but a Pod is deployed with `hostNetwork: true`, the Pod bypasses the GKE Metadata Server proxy and talks directly to the underlying GCE node VM's metadata, acquiring the node's default IAM identity instead of the intended workload identity.\n- **Autopilot Resource Ratio Enforcement**: GKE Autopilot enforces strict CPU-to-memory ratios (e.g. 1 vCPU requires between 1GiB and 8GiB RAM). Deployments specifying invalid ratios (e.g. 1 vCPU with 32GiB RAM without selecting the `MemoryOptimized` compute class) are rejected by admission validation with `InvalidResourceRatio`.\n- **Cloud Armor Policy Detachment on Service Recreation**: If a backend Kubernetes `Service` is deleted and recreated with a different name, the `GCPBackendPolicy` target reference breaks, leaving the new backend exposed without Cloud Armor WAF protection until the policy is updated.",
    },
    29: {
        "flow": '1. **Enterprise Multi-Account Landing Zone Architecture**: An enterprise cloud organization (AWS Control Tower or GCP Organization) deploys isolated AWS/GCP accounts for production, staging, identity, and shared platform tooling.\n2. **Fleet Baseline Synchronization via ArgoCD ApplicationSets**:\n   - A central Platform Operations Hub Cluster runs an ArgoCD `ApplicationSet` controller.\n   - The `ApplicationSet` utilizes a **Matrix Generator**, combining a list of registered spoke clusters (filtered by labels: `tier: production`) with Git repository directories.\n   - The generator automatically instantiates and synchronizes standardized baseline applications (monitoring, security agents, ingress controllers, RBAC policies) across all spoke clusters.\n3. **External Secrets Operator (ESO) Secret Synchronization**:\n   - The **External Secrets Operator** runs inside the tenant cluster.\n   - A `SecretStore` resource defines credentials for accessing centralized secret vaults (AWS Secrets Manager, Azure Key Vault, or HashiCorp Vault).\n   - An `ExternalSecret` resource specifies which keys to fetch. ESO queries the external cloud secret API, decrypts the secret value, and materializes a native Kubernetes `v1/Secret` inside the tenant namespace, automatically reconciling secret rotations on a 1-hour interval.\n4. **HashiCorp Vault Agent Sidecar Injection**:\n   - For applications requiring in-memory dynamic credentials without writing persistent Kubernetes Secrets to `etcd`, the Pod is annotated with `vault.hashicorp.com/agent-inject: "true"`.\n   - The Vault Mutating Webhook injects a lightweight `vault-agent` sidecar container.\n   - The Vault Agent authenticates via the Kubernetes ServiceAccount token, acquires a dynamic database lease from Vault, and writes the plain secret payload to a shared in-memory `tmpfs` volume (`/vault/secrets/config.json`).\n5. **Multi-Tenant Governance & Resource Boundaries**:\n   - Spoke tenant namespaces are governed by `ResourceQuota` (capping aggregate CPU, memory, and storage) and `LimitRange` (enforcing default container requests/limits).\n   - Pod Security Admission labels enforce `pod-security.kubernetes.io/enforce: restricted`, blocking root containers and host namespace sharing.',
        "serialization_protocols": "- **Vault HTTP/2 REST API with mTLS**: Vault Agent communicates with HashiCorp Vault clusters over TLS 1.3 using JSON payloads and Kubernetes ServiceAccount token authentication (`/v1/auth/kubernetes/login`).\n- **AWS Secrets Manager & GCP Secret Manager REST SDKs**: External Secrets Operator queries cloud provider secret manager APIs using SigV4 / OAuth2 authentication.\n- **ArgoCD ApplicationSet Generator JSON Schema**: Matrix generators parse cluster secret metadata and Git tree directory paths, rendering parameterized Application CR manifests.",
        "components": "- **ArgoCD ApplicationSet Controller**: Multi-cluster GitOps engine automating the generation and deployment of ArgoCD Applications across dynamic cluster fleets.\n- **External Secrets Operator (ESO)**: Kubernetes operator synchronizing secrets from external enterprise secret management systems into native Kubernetes Secrets.\n- **HashiCorp Vault Agent Injector**: Mutating webhook and sidecar daemon delivering dynamic, short-lived, leased credentials directly into container memory.\n- **ResourceQuota & LimitRange Enforcers**: In-tree Kubernetes admission controllers preventing multi-tenant resource starvation and noisy neighbor contention.",
        "mechanics_failures": '- **Vault Token Renewal Expiration**: If an application holds a dynamic database connection open longer than Vault\'s max lease TTL and fails to renew its token lease, Vault revokes the database credentials, causing subsequent application queries to fail with database authentication errors.\n- **External Secrets API Rate Limit Exhaustion**: Setting `refreshInterval: 10s` on hundreds of `ExternalSecret` resources causes ESO to hammer cloud secret APIs, exhausting cloud account API rate limits and triggering exponential backoff throttling across all cluster services. Refresh intervals should be tuned to $\\ge 1\\text{h}$ with webhook triggers for instant updates.\n- **ResourceQuota Deadlock during Rolling Updates**: If a namespace `ResourceQuota` is configured with `requests.cpu: "4"` and a Deployment with `replicas: 4` (each requesting 1 CPU) attempts a rolling update with `maxSurge: 25%`, the Deployment requires 5 CPUs during the transition. The rolling update deadlocks in `Pending` because the surge pod exceeds the quota ceiling.',
    },
}
